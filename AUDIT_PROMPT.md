<task>
You are auditing this ENTIRE worktree — a clean-room Rust simulator of Slay the Spire (`packages/engine-rs`, ported from decompiled Java at `decompiled/java-src/com/megacrit/cardcrawl/`) plus all the process machinery around it — on branch `codex/engine-deep-audit` (state = PR #162, the 667-row verification sweep; 2,989 lib tests green). The engine is the center of gravity, but the deliverable is a complete map of what stands between the current state and a FINISHED sim (GOAL.md Definition of Done): every issue, boundary, abstraction, and process gap.

Context you must internalize before starting:
- Read `docs/goal/GOAL.md`, `docs/goal/FINDINGS.md`, and `AGENTS.md` first. The decompiled Java is ground truth for behavior.
- The engine's future consumers are being REBUILT from scratch: the SwiftUI monitor app and the Python training stack (`packages/training/`) are both out of date and will be replaced. Therefore existing training/obs/monitor contracts are NOT constraints — you may propose breaking redesigns of `training_contract.rs`, `obs.rs`, and the PyO3 surface. Do not spend effort preserving compatibility with `packages/training/`.
- The primary future workload: PUCT/MCTS self-play where ONE immutable content registry serves MANY combat/run states stepped in parallel (multi-thread batch rollouts on an M4 Mac), plus trace replay for parity checking.

Audit for, in priority order:
1. BUGS and edge cases — behavior that deviates from the decompiled Java, RNG stream misuse (wrong stream, missing/extra draws), action-queue ordering errors, i16 status saturation/overflow, death/exhaust/power-removal reentrancy, X-cost and free-play edge cases, intangible/artifact ordering, stance-change loops.
2. STALE/WEAK TESTS — tests that (a) encode pre-sweep misremembered behavior instead of source-derived values (see FINDINGS F1 for the known failure pattern), (b) are tautological (assert what the implementation does rather than what Java does), (c) couple to private internals (e.g. `hidden_effect_value`, direct state pokes that skip the engine pipeline), (d) duplicate coverage across the `test_*_wave*.rs` files (some are 5,000+ lines), or (e) would stay green if the behavior regressed.
3. POOR ABSTRACTIONS — known suspects to evaluate and extend: `RunEngine` god-struct with per-relic `pending_*` bool fields (run.rs ~line 758); dual relic representation (`state.relics: Vec<String>` vs `relic_flags` bitset/counters); string IDs in hot combat state (`enemy.id: String`, relic strings compared by `has_relic("...")` every damage tick); the 24 remaining `complex_hook` cards vs the declarative effect interpreter; single out-of-combat RNG stream on `RunEngine` (Java has 13 distinct persistent streams — this is FINDINGS F2 and blocks run-level parity); trace emission hardcoding relic `counter: -1` (trace.rs ~line 796).
4. BATCHING/PARALLELISM READINESS — design review for "1 engine definition, N states": verify content/registry is fully immutable and shareable (&'static or Arc, no interior mutability), state clone is minimal (bench says 569ns — find what's still heap-allocating: Strings, Vecs, move_history), propose a `step`-many API shape for parallel PUCT workers, identify allocation churn in the hot turn loop, confirm determinism holds per-state with no thread-shared RNG or globals. Quantify with the existing criterion benches (`benches/`; binaries need `DYLD_FRAMEWORK_PATH=/Applications/Xcode.app/Contents/Developer/Library/Frameworks` because PyO3 is currently an unconditional dependency — itself a finding: it should be feature-gated).
5. CONTRACT REDESIGN — sketch a v2 boundary: sim-core crate with zero Python/obs/search deps; versioned, additive-tolerant serialization for obs/training tokens and the trace schema, so a new field never breaks a consumer with a TypeError again (this exact failure exists today: `power_cards_played_this_combat` broke the old Python contract).
6. DEAD CODE PURGE — the crate should contain only what the engine and its boundary layers need: sim core, content, RNG, trace emission, and deliberately designed public APIs for obs/search/replay consumers. Hunt and DELETE (not register): dead functions and constants (the compiler already warns on several, e.g. `PLAYER_POWER_STATUSES`, `active_player_power_count` in `powers/registry.rs`), legacy compat shims and wrappers kept for consumers that no longer exist (e.g. "compat event catalog wrappers"), unused fields, unreachable branches, superseded helpers, and any code whose only callers are other dead code. While doing this, tighten the API surface: audit what is `pub` vs `pub(crate)` — the crate's public API should be a small, intentional set of entry points (construct engine, step actions, snapshot/clone state, emit trace, enumerate legal actions), not the current organically-grown surface.
7. DOCS AND AUDIT-RESULT REFRESH — the docs tree carries legacy content that predates the verification sweep and now contradicts the code: `docs/work_units/audit-reports/` (April 2026 imports, explicitly labeled a pre-sweep "fix quarry"), stale sections of `docs/work_units/parity-deviations-register.md`, vault notes, and any README/doc describing superseded flows or dead code you removed in area 6. Legacy docs are NOT archived — DELETE them outright (git history is the archive). The end state: a docs tree where every remaining file accurately describes the current engine, plus your new findings register. If a stale doc contains a still-true fact not recorded anywhere else, fold that fact into the appropriate current doc (or your register) before deleting the file. If it duplicates the ledger/FINDINGS, delete it and let the canonical file stand. `docs/goal/` (GOAL, FINDINGS, UNITS, ledger, masks, BENCHMARKS, TOOLING) is canonical — refresh other docs to agree with it, never the reverse.
8. PROCESS/TOOLING AUDIT — the skills, prompts, and scripts that drive agent work are themselves stale now that the sweep is done. Audit: `AGENTS.md` (it still describes the pre-sweep phase — "all rows starting unverified", "work = pick rows, verify, flip", test count "2255+ as of 2026-07-10"; the loop it prescribes is COMPLETE and the next phase is corpus/oracle closure, run-layer RNG streams, and boundary cleanup), `~/.codex/prompts/goal.md` (same staleness — if you cannot write outside the repo, put the proposed replacement text in your register), every script in `scripts/` (dead scripts, scripts referencing archived flows, hooks in `scripts/hooks/`), and `.claude/` config. Factual staleness (wrong counts, done-phases described as pending, references to deleted files) — fix directly. Changes to the loop CONTRACT itself (what agents are told to do next) — write a proposed replacement in the register, flagged for human approval, do not unilaterally redefine the mission.
9. SIM-COMPLETION GAP MAP — the capstone deliverable. Write `docs/work_units/sim-completion-map.md`: the full remaining path from this commit to GOAL.md's Definition of Done, organized by layer (combat sim / run layer / RNG streams / content coverage incl. the missing event+Neow+map ledger rows / trace oracle+corpus / boundary APIs for the rebuilt training+viz consumers / test suite health). For each gap: current state with evidence, what done looks like, blocking dependencies, effort S/M/L, and which EDA findings feed it. This document should let a human sequence the next two months of units without re-deriving anything.

Known findings to NOT re-derive (already registered): FINDINGS F1-F8, Neow blessing mapping (F4), potion-slot placeholder (F5), differ blind spots (F6). Reference them if related, don't re-report them.
</task>

<structured_output_contract>
Write the full register to `docs/work_units/audit-reports/engine-deep-audit.md` in this worktree. Format: one entry per finding, ID'd `EDA-NNN`, banded P0 (bug/correctness) / P1 (perf, batching, contract) / P2 (test hygiene, abstraction debt). Each entry: file:line evidence, one-paragraph explanation, smallest repro or failing-test sketch, proposed fix, effort S/M/L. Open the file with a ranked top-10 summary table.
In your final chat answer, return ONLY: the top-10 table, counts per band, a diffstat summary of the cleanup commits (areas 6-8), and a one-paragraph pointer to the gap map. Highest-value findings first.
</structured_output_contract>

<action_safety>
Two distinct work modes — keep them in separate commits:
- Areas 1-5 are register-and-defer: do NOT fix findings. The only code you may add is minimal failing-repro tests in a single new file `packages/engine-rs/src/tests/test_audit_repros.rs`, each `#[ignore]`d with its EDA id in a comment.
- Areas 6-8 are execute-now: dead-code deletion, docs refresh, and process/tooling staleness fixes are real changes on this branch, committed in small reviewable units (`cleanup(code): ...` / `cleanup(docs): ...` / `cleanup(process): ...`), separate from the register commit. Area 9's gap map is its own commit. Dead CODE is deleted outright — no `#[allow(dead_code)]`, no commenting out, no backwards-compat stubs. Superseded DOCS are also deleted outright (fold any still-true, otherwise-unrecorded facts into a current doc first; git history is the archive). If a deletion candidate is load-bearing for behavior parity, it is not dead — leave it and register the confusion as a finding instead.
Never modify: `data/traces/java/`, `decompiled/`, `packages/training/`, `logs/`, `runs/`, `docs/goal/` (except FINDINGS cross-references), existing tests (except deleting tests whose ONLY subject was code deleted in area 6 — cite the deleted symbol in the commit message). `./scripts/test_engine_rs.sh test --lib` must stay green after every commit; the count may only drop by exactly the tests removed alongside their deleted subjects, and each such drop must be accounted for in the commit message.
</action_safety>

<grounding_rules>
Every P0 claim must cite the decompiled Java file that contradicts the Rust, or a deterministic repro. Do not present inferences as facts; label hypotheses. A suspicion without evidence goes in a final "leads not run down" list, not the register.
</grounding_rules>

<dig_deeper_nudge>
After each confirmed issue, check its whole family: the same RNG stream misuse in sibling enemies, the same saturation risk in every i16 status write, the same String-compare pattern in every relic hook. Family sweeps are where this codebase hides bugs.
</dig_deeper_nudge>

<completeness_contract>
Cover all nine audit areas before finalizing; do not exhaust the time budget on area 1. The gap map (area 9) is mandatory — an audit without it fails the task. If an area yields nothing, say so explicitly with what you checked.
</completeness_contract>
