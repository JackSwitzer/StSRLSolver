# Parity Audit Report — 2026-04-11

## CRITICAL (wrong gameplay outcomes)

| # | Entity | Issue | Fix |
|---|--------|-------|-----|
| 1 | **Reaper** (Ironclad) | `exhaust: true` — Java does NOT exhaust | Set `exhaust: false` on both variants |
| 2 | **Setup** (Silent) | Uses `PutOnBottomAtCostZero` — Java puts on TOP of draw | Change to `PutOnTopOfDraw` (or add variant) |
| 3 | **Expunger+** (Watcher) | `base_damage: 9` — Java upgraded = 15 | Set `base_damage: 15` on Expunger+ |
| 4 | **Empty Body+** (Watcher) | `base_block: 11` — Java upgraded = 10 | Set `base_block: 10` |
| 5 | **Genetic Algorithm** (Defect) | `base_block: 0` — Java base = 1 | Set `base_block: 1` |
| 6 | **Pressure Points** | ID `"PressurePoints"` — Java ID = `"PathToVictory"` | Fix ID (or add alias) |
| 7 | **Flurry of Blows** | ID `"Flurry"` — Java ID = `"FlurryOfBlows"` | Fix ID |
| 8 | **Tranquility** | ID `"Tranquility"` — Java ID = `"ClearTheMind"` | Fix ID |
| 9 | **Fasting** | ID `"Fasting"` — Java ID = `"Fasting2"` | Fix ID |

## HIGH (logic bugs)

| # | Entity | Issue | Fix |
|---|--------|-------|-----|
| 10 | **Burning Blood + Black Blood** | Both fire on victory (should be replacement) | Guard: skip Burning Blood if Black Blood present |
| 11 | **Disarm** (Ironclad) | Empty `effect_data` — strength reduction not declarative | Add `AddStatus(SelectedEnemy, LOSE_STRENGTH, Magic)` |
| 12 | **Combust** (Power) | HP loss hardcoded to 1 — Java scales with stacks | Change `combust_hp_loss` to `amt` in hook |
| 13 | **Biased Cognition** | Multiple casts don't stack focus loss/turn | Change `SetStatus(Fixed(1))` to `AddStatus(Fixed(1))` for BIASED_COG_FOCUS_LOSS |

## TEST COVERAGE GAPS

**37 weak assertions** using `<`/`>=` instead of exact `==` — these mask wrong values.

**30 critical untested entities** (top 10):
1. Corruption, 2. Barricade, 3. Echo Form replay, 4. Double Tap replay, 5. Demon Form,
6. Noxious Fumes, 7. After Image, 8. Burst replay, 9. Blade Dance, 10. Whirlwind

**40 colorless cards** with zero tests at all.

**Zero interaction tests** for: Strength+multi-hit, Vulnerable+multi-hit, Corruption+skills, Barricade+block retention.

**11 upgrade behavior changes** untested at runtime (Limit Break+, Armaments+, Calculated Gamble+, etc.).

## MIGRATION GAPS (functional but not declarative)

~70 cards have empty `effect_data` for active play-time effects — they work via old string tags or complex_hooks but aren't migrated to the declarative system yet.

3 dead cards in registry: Impulse, Discipline, Unraveling (removed from Java/Python).

## SUMMARY

- **9 CRITICAL** value/ID bugs that produce wrong gameplay
- **4 HIGH** logic bugs
- **37 weak test assertions** masking potential bugs
- **30 critical entities** with zero behavioral tests
- **~70 cards** still need declarative migration
