# Parity Deviations Register

Last updated: 2026-04-21 (Stage H pre-merge audit fleet — 12 new rows D160-D171).
Branch: `claude/cycle-1-test-suite-refresh`

This is the standing register for every known Java↔Rust gameplay deviation. The companion of [parity-status.md](./parity-status.md) (which tracks the headline state) and [comprehensive-audit-2026-04-17.md](./comprehensive-audit-2026-04-17.md) (which lists the open work).

The audit phrase to remember: **"some deviations are OK, many are not."** This doc is what enforces that distinction explicitly rather than relying on tribal knowledge.

## Stage B Fleet Audit — 2026-04-21

Eleven Opus 4.7 parity-audit subagents ran in parallel. Reports in [audit-reports/](./audit-reports/) — these are the source-of-truth for per-area findings. Only the top-priority / structurally-load-bearing items are promoted into the register table below (IDs `D88`+); the long tail stays in the per-area reports.

| Area | Report | Findings | Status |
|---|---|---:|---|
| Act 1 enemies | [enemies-act1.md](./audit-reports/enemies-act1.md) | 53 (E*A1) | complete |
| Act 2/3/4 enemies | [enemies-act2-act3-act4.md](./audit-reports/enemies-act2-act3-act4.md) | 41 (E*A2/3/4) | complete |
| Relics | [relics.md](./audit-reports/relics.md) | 21 (R1-R21) + 21 missing | complete |
| Watcher stances/scry/mantra | [watcher-stances-scry-mantra.md](./audit-reports/watcher-stances-scry-mantra.md) | 14 (WS1-WS14) | complete |
| Watcher cards | [watcher-cards.md](./audit-reports/watcher-cards.md) | 20 (W1-W20); 6 new bugs, 5 cross-ref to D-rows, 9 intentional/clean | complete |
| Other-class cards | [other-class-cards.md](./audit-reports/other-class-cards.md) | 14 (OC1-OC14) | complete |
| Powers / buffs / debuffs | [powers-buffs-debuffs.md](./audit-reports/powers-buffs-debuffs.md) | 50 (P1-P50) | complete |
| Potions | [potions.md](./audit-reports/potions.md) | 13 (PT1-PT13) | complete |
| Damage / engine flow | [damage-engine-flow.md](./audit-reports/damage-engine-flow.md) | 14 (DM1-DM14) | complete |
| Run / map / events / shop | [run-map-events-shop.md](./audit-reports/run-map-events-shop.md) | ~42 (RN-*) | complete |
| Tests / types / API | [tests-types-api.md](./audit-reports/tests-types-api.md) | 20 (T1-T20) | complete |

**Scale:** ~302 fresh findings across 11 completed reports. ~41 promoted to the register below as `D88`–`D128`. Watcher cards surfaced 6 new bugs (Deva Form stacking, Pressure Points pipeline bypass, Swivel non-stacking, Miracle retain, Collect+ upgrade, Signature Move dedup) plus 5 cross-refs to existing D-rows.

**Headline (by training impact, Watcher-weighted):**
1. **Watcher HolyWater boss relic gives 15 block instead of 3 free energy** (D88 / R1) — one of 8 Watcher-priority relics, net-broken.
2. **Watcher Fasting card never deducts energy** (D89 / WS11) — major balance bug; Rust grants the bonuses, never applies the cost.
3. **Pressure Points / TriggerMarks bypasses HP-loss pipeline** (D124 / W3) — ignores Intangible, Buffer, on-loseHP hooks; Watcher Mark build damage math wrong.
4. **Miracle + Collect+ compound bug** (D126 + D127 / W10 + W11) — Miracle doesn't retain AND Collect+ spawns un-upgraded Miracles, jointly breaking Collect strategy.
5. **MalleablePower never resets each turn** (D90 / P1) — accumulates block-per-hit across multi-turn fights vs Chosen / Stone Wyrm.
6. **`deal_damage_to_player` bypasses the entire damage pipeline** (D91 / DM1) — Wrath 2x / Vuln 1.5x / Intangible cap / Torii / Tungsten / Plated Armor / Static Discharge all skipped when effects target `Player`.
7. **44 orphan test files (~230 tests) never compiled** (D92 / T1) — silent test-coverage loss.
8. **`combat_engine_from_snapshot` drops orb contents + combat_over** (D93 / T2) — breaks Defect PUCT from snapshots.
9. **Event pool never removes used events** (D94 / RN-EV-01) — same event can repeat, distorts deck-shaping.
10. **Shrines defined but unreachable from `enter_event`** (D95 / RN-EV-02) — 17 shrine events never trigger.
11. **D1 RNG miss universal across Acts 2/3/4** (D1 updated) — every `roll_*` accepts `_num` unused.
12. **Orb damage routes through NORMAL damage-type triggers** (D96 / OC2) — Curl Up / Malleable / Slow all wrongly fire on orb hits.

## Categories

Every entry is exactly one of:

- **`intentional`** — RL-driven design choice, documented and accepted. Will not be reverted unless we explicitly decide to. Cite [DESIGN_DECISIONS.md](../../packages/engine-rs/DESIGN_DECISIONS.md).
- **`bug`** — engine differs from Java in a way that is wrong and should be fixed. Has a target fix (audit doc reference + estimated scope).
- **`deferred`** — known divergence we have explicitly chosen not to address yet, with a tracked condition for when to revisit (e.g. "after MLX policy is trained").
- **`unverified`** — suspected divergence; no proof yet. Rotates to one of the three above after investigation.

## Workflow

1. **Add a row** whenever you discover a divergence, even before you understand it. Better to register it as `unverified` than to forget.
2. **Move rows up the kanban** as you investigate: `unverified → bug` (with fix in flight) or `unverified → intentional` (with DESIGN_DECISIONS update) or `unverified → deferred` (with revisit condition).
3. **Close rows** by linking the commit that fixes them; the row stays in the doc with a `closed:` tag so we can see the history. Do not delete closed rows for at least one release cycle — the historical record is the audit trail.
4. **Per-PR check**: any new gameplay-affecting code must either match Java exactly or land here as `intentional` with rationale. Reviewers should reject PRs that introduce silent divergence.

## Register

| ID | Title | Category | Rust ref | Java ref | Notes / fix scope | Status |
|---:|-------|----------|----------|----------|-------------------|--------|
| D1 | Enemy AI consumes no RNG | bug | `enemies/mod.rs:822` | `AbstractMonster.java:466` | Phase 1 threaded `&mut StsRandom` through `roll_next_move` (commits `f0a0bfd4` + `d91c7039`). Phase 2 (Stage D, 2026-04-21) rewrote per-enemy dispatchers across Act 1 (10 enemies), Act 2 (11 enemies + Champ/Collector bosses), Act 3 (17 enemies + Awakened One/Donu/Deca/Time Eater), and Act 4 (Spire Shield/Spear) to branch on `num` drawn from `aiRng.random(99)` and honor Java's `lastMove`/`lastTwoMoves` anti-repeat guards. Remaining gaps — deferred secondary-roll retries, aliveCount-dependent Centurion/Mystic branches, some ascension-gated thresholds — are documented inline per enemy dispatcher. | **fixed** for the core branch logic (Stage D); deferred sub-rolls and engine-context-dependent branches noted in enemy dispatcher comments |
| D2 | `expected_hp_loss` reported as actual HP | bug | `run_replay.py:283` | n/a | Solver reports a probabilistic search statistic as if it were an integer played-out outcome. Audit §1.2 scopes per-turn replanning loop to fix. | **open** |
| D3 | `solver.run_combat_puct` stops at TimeCap not Converged | bug | `search.rs:351` (hard_visit_cap, time_cap) | n/a | Audit §1.3: iterative-deepening to target so search always runs until it matches/beats human or hits a hard wall. | **open** |
| D4 | Transient does not auto-fade | not-a-bug | `enemies/act3.rs::roll_transient` + `combat_hooks.rs:53-62` | `Transient.java::getMove` | Investigated 2026-04-18: Fading IS decremented every enemy turn-start at `combat_hooks.rs:53-62` and Transient self-destructs at turn 5 (4 attacks dealing 40+50+60+70 = 220 dmg total). The `failed` status on F38 of the golden run is a downstream artifact of D10 (Pandora's Box deck transforms not simulated) — solver fights with near-starter deck and can't block 220 dmg over 4 turns, while the recorded human used a developed deck (Wreath of Flame burst, established blockers). Closing as not-a-Transient-bug; the same combat will resolve cleanly once D10 lands or once the solver has a trained policy. | **closed (not-a-bug)** |
| D5 | Encounter catalog (Python) is incomplete | bug | `packages/training/encounters.py` | n/a | Was Act-1-only (15 entries), drove 18/23 unsupported on the golden run. Extended to Acts 2-4 in commit `88234d5a` (now 32 entries; covers all 23 combats in the WATCHER A0 winning seed). | **closed by `88234d5a`** |
| D6 | Card hook surface: 71/352 cards on `complex_hook` legacy | bug | `complex_hook` files in `cards/` | various | Pre-existing audit baseline finding; not tonight's work. Lesson Learned is the only one that materially hits Watcher decks (audit §1.6). | **deferred** (revisit when training data shows we draw any of the 5) |
| D7 | Neow always offers 4 choices | intentional | `engine.rs::neow_options` | `NeowEvent.java` | Java gates options by deck/relic state; we expose all 4 for the RL surface. | **closed (intentional)** ([DESIGN_DECISIONS.md:8-28](../../packages/engine-rs/DESIGN_DECISIONS.md)) |
| D8 | NoteForYourself uses runtime-internal storage | intentional | `events/exordium.rs::note_for_yourself` | `NoteForYourselfEvent.java` | Java persists to the player profile JSON; we keep it in the run state since the RL pipeline doesn't have a player profile. | **closed (intentional)** ([DESIGN_DECISIONS.md:30-57](../../packages/engine-rs/DESIGN_DECISIONS.md)) |
| D9 | Match and Keep! is an indexed minigame | intentional | `events/exordium.rs::match_and_keep` | `MatchAndKeepEvent.java` | Java has a 12-card grid with click-to-flip UI; we expose it as 6 indexed pairs. | **closed (intentional)** ([DESIGN_DECISIONS.md:59-79](../../packages/engine-rs/DESIGN_DECISIONS.md)) |
| D10 | Pandora's Box random transforms not simulated in `run_parser` | bug | `packages/training/run_parser.py::reconstruct_combat_cases` | `PandorasBox.java::onEquip` | Boss relic transforms all Strikes/Defends into random commons; we cannot predict the random commons from the seed alone in Python. Causes deck mismatch warning on golden run after F16. Workaround: ignore the diff and let solver play with approximate deck. | **deferred** (revisit when we add seed-aware random reconstruction) |
| D11 | Adaptation upgrade source unaccounted for in golden run | unverified | `packages/training/run_parser.py` | various | Adaptation appears as `Adaptation+1` in master_deck but no `SMITH:Adaptation` campfire choice exists. Likely a relic-driven upgrade (Frozen Eye? Apotheosis?) we are not simulating. Single-card discrepancy. | **open** |
| D12 | Multi-enemy intent stream order vs Java | unverified | `enemies/mod.rs::roll_next_move` (called per enemy) | `AbstractRoom.endTurn` | We now correctly advance ai_rng once per `roll_next_move` (commit `f0a0bfd4`). Need to verify the ORDER in which we call roll_next_move per enemy matches Java's `monsters.monsters.forEach`. Multi-enemy tests in `test_ai_rng_parity.rs` cover stream-advancement but not absolute order. | **open** |
| D13 | Energy hardcoded to 3 in run_replay | bug | `run_replay.py:217` | n/a | `RustCombatEngine(..., 3, ...)` — should pull from .run / character defaults so Defect (3 base) and Watcher with energy relics (Cursed Key etc) get correct value. Currently OK for Watcher base but blocks future characters and Defect. | **open** (audit §1.4) |
| D14 | Neow `TEN_PERCENT_HP_LOSS` not applied to entry HP | bug | `run_parser.py:283` (TODO marker) | `NeowReward.java::TEN_PERCENT_HP_LOSS` | Floor 1 entry HP stays at full max instead of `0.9 * max_hp`. One-floor off-by-one. | **open** (audit §1.5, comment landed) |
| D15 | Neow `ONE_RANDOM_RARE_CARD`, `BOSS_RELIC` not handled | bug | `run_parser.py::_apply_neow` | `NeowReward.java` | Reconstruction skips with a warning. Affects different seeds than the WATCHER A0 winning run. | **open** (audit §1.7) |
| D16 | Cut Through Fate draws `Magic` cards (2/3) instead of fixed 1 | bug | `packages/engine-rs/src/cards/watcher/cutthroughfate.rs:9-10,17-18` | `decompiled/java-src/com/megacrit/cardcrawl/cards/purple/CutThroughFate.java:32-33` | Java does `ScryAction(magicNumber)` THEN `DrawCardAction(p, 1)` — always draws exactly 1 card; Rust puts `DrawCards(A::Magic)` (=2 base, 3 upgraded), which extra-draws the wrong number. Card text says "Scry 2 / Draw 1." Fix: change first effect to `DrawCards(A::Fixed(1))`. Effect order also flipped (Rust: Draw→Scry; Java: Scry→Draw — only matters for Foresight Scry-first interactions). | **open** |
| D17 | Spirit Shield computes block as `hand_size * N` repeats instead of `(hand_size - 1) * magic` | bug | `packages/engine-rs/src/cards/watcher/spiritshield.rs:11-13,21-24` | `decompiled/java-src/com/megacrit/cardcrawl/cards/purple/SpiritShield.java:32-42` | Java: `baseBlock = (hand.size - 1) * magicNumber` (excludes Spirit Shield itself), then ONE GainBlock with full Dexterity multiplier. Rust: 3 (or 4 upgraded) repeated `GainBlock(A::HandSize)` calls — inflates by including Spirit Shield itself, applies Dex multiplier per call (matters when Frail/Dex change mid-effect — currently they don't, so Dex math is OK), and overall arithmetic is wrong: 4 cards in hand → Java gives `(4-1)*3=9`, Rust gives `4*3*3=36` (DEFINITELY off by huge margin). Fix: introduce `A::HandSizeMinusSelfTimesMagic` or `A::HandSizeAtPlayMinusOne` and a single GainBlock effect. | **open** |
| D18 | Burn `Burn+` magic should be `4` per Java but the `4` is Burn+ self-damage on end-of-turn-in-hand | unverified | `packages/engine-rs/src/cards/status.rs:24-34` | `decompiled/java-src/com/megacrit/cardcrawl/cards/status/Burn.java:25-26,49-55` | Rust registers Burn (magic=2) and Burn+ (magic=4) and routes them through `BURN_TRIGGERS` → `EndTurnHandRule::Damage`. Need to confirm the rule pulls from `magic` and not a hardcoded 2. Also Java sets `dontTriggerOnUseCard=true` so the `use()` path only fires from cardQueue end-of-turn — not when a card-play would otherwise consume it. Risk of double-trigger if Rust ever lets player "play" Burn (it's `cost=-2` so unplayable, so likely safe). | **open** |
| D19 | Wallop block uses `TotalUnblockedDamage` (cumulative across card play) vs Java `target.lastDamageTaken` (single-target HP loss after Intangible cap) | unverified | `packages/engine-rs/src/cards/watcher/wallop.rs:11,20` | `decompiled/java-src/com/megacrit/cardcrawl/actions/watcher/WallopAction.java:38-43` | Wallop is single-target so the two should agree on normal enemies, but: vs `Intangible`, Java caps lastDamageTaken at 1 (so Wallop blocks for 1); Rust's `TotalUnblockedDamage` would also be 1 if intangible processing happens before the GainBlock effect — needs verification. Also if a relic/power deals incidental damage during the Wallop card play (rare but conceivable, e.g. Necronomicon trigger), the cumulative counter would over-count. Low-impact for MVP. | **open** |
| D20 | Conjure Blade Chemical X `+2` not applied | unverified | `packages/engine-rs/src/cards/watcher/conjureblade.rs:10,18` | `decompiled/java-src/com/megacrit/cardcrawl/actions/watcher/ConjureBladeAction.java:34-37` | Java's ConjureBladeAction adds +2 to the Expunger hits if player has Chemical X relic. Rust uses `XCostPlus(0)`/`XCostPlus(1)` which only applies the +1 upgrade bonus, not the Chemical X relic. Same class of bug as Skull Bash / Streamline X-cost cards. Need to confirm `XCostPlus` honors Chemical X separately or whether it depends on `XCost` already including it. Closely tied to D-row-undefined Chemical-X-on-X-cost-cards general issue. | **open** |
| D21 | Master of Strategy upgrade adds `+1` to magic (Java + Rust both do) but vanilla unmodded StS is "Draw 3 cards. Exhaust." with NO upgrade | not-a-bug | `packages/engine-rs/src/cards/colorless/master_of_strategy.rs:14` | `decompiled/java-src/com/megacrit/cardcrawl/cards/colorless/MasterOfStrategy.java:35-40` | Both engines DO upgrade the card to Draw 4 (`upgradeMagicNumber(1)`). This is consistent between Rust and Java; no divergence. Recording for the audit because `Master of Strategy+` exists in the registry — confirm RL never sees `Master of Strategy+` since it's distributed via `MasterOfStrategy` event which doesn't offer upgraded cards. Closing as not-a-bug. | **closed (not-a-bug)** |
| D22 | Pray adds plain `Insight` to draw pile, not `cardsToPreview.makeStatEquivalentCopy()` | not-a-bug | `packages/engine-rs/src/cards/watcher/pray.rs:10,19` | `decompiled/java-src/com/megacrit/cardcrawl/cards/purple/Pray.java:25,31` | Java `MakeTempCardInDrawPileAction` is constructed with `cardsToPreview.makeStatEquivalentCopy()` — but `cardsToPreview = new Insight()` is a fresh unupgraded Insight every time, regardless of Pray's own upgrade state. So Rust adding plain "Insight" matches Java exactly. Closing. | **closed (not-a-bug)** |
| D23 | FearNoEvil intent check matches `EnemyAttacking` cond — verify the cond covers all 4 ATTACK intent variants from Java | unverified | `packages/engine-rs/src/cards/watcher/fearnoevil.rs:10,19` | `decompiled/java-src/com/megacrit/cardcrawl/actions/watcher/FearNoEvilAction.java:25` | Java triggers Calm stance only if intent is one of `ATTACK`, `ATTACK_BUFF`, `ATTACK_DEBUFF`, `ATTACK_DEFEND`. Rust's `Cond::EnemyAttacking` needs to include all four. Search `effects/interpreter.rs` and `condition.rs` for the EnemyAttacking implementation to confirm. | **open** |
| D24 | Lesson Learned upgrades random card from `draw + discard`, not from full deck | unverified | `packages/engine-rs/src/cards/watcher/lessonlearned.rs:7-10` | `decompiled/java-src/com/megacrit/cardcrawl/actions/watcher/LessonLearnedAction.java` | Need to read `LessonLearnedAction.java` to confirm whether Java upgrades a card from the master deck or just from in-combat piles. Rust uses `UpgradeRandomCardFromPiles(&[Draw, Discard])`. If Java upgrades from the master deck (most likely — kills "permanently" upgrade a card), Rust is wrong: a card might be in the player's hand when killed and miss the upgrade pool, plus the upgrade should persist post-combat (which an in-combat-pile upgrade may not do). | **open** |
| D25 | Establishment+ magic is `1` (same as base), upgrade only sets `innate` flag | not-a-bug | `packages/engine-rs/src/cards/watcher/establishment.rs:14-15` | `decompiled/java-src/com/megacrit/cardcrawl/cards/purple/Establishment.java:30-37` | Java upgrade only flips `isInnate=true` and rewrites description; magicNumber stays 1. Rust matches (magic=1 for both Establishment and Establishment+, innate trait set in `runtime_meta.rs:109`). Closing. | **closed (not-a-bug)** |
| D26 | Boot — bonus mis-applied when target has block | bug | `packages/engine-rs/src/engine.rs:2612` | `decompiled/java-src/com/megacrit/cardcrawl/relics/Boot.java:26-32` | **Fixed in commit `19efe5f6`**. Gate on raw `damage < 5`, set `hit_damage = 5` (not `block + 5`). Matches Java semantics. 4 test call sites updated to reflect Java-correct numbers (Shiv+Boot vs 2 block: 5→3 HP; Sword Boomerang combo: 15→13). | **closed** (`19efe5f6`) |
| D27 | Pandora's Box — TRANSFORM side landed, random-common fill deferred | partial-fix | `packages/engine-rs/src/run.rs::add_relic_reward` (commit `a2104416`) | `decompiled/java-src/com/megacrit/cardcrawl/relics/PandorasBox.java:48-70` | Commit `a2104416` (2026-04-18) added the equip hook: on `add_relic_reward("Pandora's Box")` / `"PandorasBox"`, all `Strike_{R,G,B,P}` and `Defend_{R,G,B,P}` are removed from master_deck. What's still missing: Java replaces each removed basic with a random common from the class pool (`returnTrulyRandomCard()`). Filling the slots needs the class-common card pool helper + a seeded `card_random_rng` stream (D52). Until D52 lands, post-Pandora runs have smaller starter decks. Regression tests added in `run.rs::pandora_box_*`. | **partial-fix** (commit `a2104416`) |
| D28 | Astrolabe — completely unimplemented | bug | not present | `decompiled/java-src/com/megacrit/cardcrawl/relics/Astrolabe.java:30-73` | No registration. Java transforms 3 selected cards into upgraded random cards on equip. Fix: add reward-screen card-transform hook. | **open** |
| D29 | Empty Cage — completely unimplemented | bug | not present | `decompiled/java-src/com/megacrit/cardcrawl/relics/EmptyCage.java:30-72` | Java removes 2 selected cards from deck on equip. Rust has no implementation. Fix: add card-purge selection in run flow. | **open** |
| D30 | Calling Bell — unimplemented | bug | not present (only catalog) | `decompiled/java-src/com/megacrit/cardcrawl/relics/CallingBell.java:35-58` | No registration. Java grants Common+Uncommon+Rare relics + CurseOfTheBell on equip. Fix: extend reward generation. | **open** |
| D31 | Frozen Eye — passive missing | bug | not present (only catalog at `obs.rs:43`) | `decompiled/java-src/com/megacrit/cardcrawl/relics/FrozenEye.java:8-25` | Pure-passive relic; Java's contract is enforced in `DrawPile.peek` / hand display. Rust never exposes top-of-deck. Fix: add deterministic peek API gated on `has_relic("Frozen Eye")`. Cross-ref D11 (Adaptation upgrade source mystery — possibly Frozen Eye via card-peek). | **open** |
| D32 | Cursed Key — +1 energy missing | bug | `packages/engine-rs/src/relic_flags.rs:87` (flag only) | `decompiled/java-src/com/megacrit/cardcrawl/relics/CursedKey.java:63-65` | Java `onEquip` does `++energyMaster` (permanent +1 energy). Rust's `add_relic_reward` (`run.rs:2205`) sets the flag but never bumps `max_energy`. Also Java's `onChestOpen` adds curse — no chest-open hook in Rust. Fix: bump max_energy on equip; add chest-open curse insertion. Cross-ref D13 (energy hardcoded in run_replay). | **open** |
| D33 | Omamori — counter never decrements; curse-block never fires | bug | `packages/engine-rs/src/relic_flags.rs:50,93,123` | `decompiled/java-src/com/megacrit/cardcrawl/relics/Omamori.java:34-42` | Counter `OMAMORI_USES` is initialized to 2 but no code consumes it. Curses are added unconditionally in `add_card_reward`. Java decrements `counter` on each curse and refuses if counter > 0. Fix: add `try_negate_curse()` call before curse insertion paths. | **open** |
| D34 | Frozen Egg 2 — only upgrades reward-path cards | unverified | `packages/engine-rs/src/run.rs:2168-2202` | `decompiled/java-src/com/megacrit/cardcrawl/relics/FrozenEgg2.java:36-45` | Java's `onObtainCard` fires for ANY card added (events, shrines, prismatic shard, Bottled cards). Rust's `upgrade_reward_card_if_needed` is invoked only inside `add_card_reward` (line 2152). Other `deck.push` sites (`run.rs:2579,2943,3390,3418,3522`) do not consult Frozen Egg. Fix: route every deck-add through a single `obtain_card()` helper. Cross-ref D11 (Adaptation upgrade — could be Frozen Egg upgrading the NoteForYourself event-added Adaptation if path were wired). | **open** |
| D35 | Centennial Puzzle — Java has static-field combat-leak quirk | intentional | `packages/engine-rs/src/relics/defs/centennial_puzzle.rs:14-23` | `decompiled/java-src/com/megacrit/cardcrawl/relics/CentennialPuzzle.java:17` | Java's `usedThisCombat` is a `private static boolean` — known StS code smell. Rust correctly uses per-combat state via `EffectState`. **No fix required.** | **closed (intentional)** |
| D36 | Sacred Bark — re-equip behavior model differs from Java | intentional | `packages/engine-rs/src/potions/mod.rs:209,223,580` | `decompiled/java-src/com/megacrit/cardcrawl/relics/SacredBark.java:24-28` | Java calls `initializeData()` once at equip time, baking 2x into existing potions. Rust evaluates `has_relic("SacredBark")` at use time. Functionally equivalent for normal play. Flag for Neow-rerolls or Sozu interactions. | **closed (intentional)** |
| D37 | Medical Kit — runtime exhaust path duplicates Java but bypasses regular play pipeline | unverified | `packages/engine-rs/src/engine.rs:1599,1842` and `effects/hooks_can_play.rs:11` | `decompiled/java-src/com/megacrit/cardcrawl/relics/MedicalKit.java:30-36` | Rust correctly makes Status playable + exhausts. Java sets `card.exhaust = true` AND `action.exhaustCard = true`; Rust hand-removes and pushes to `exhaust_pile` directly (line 1844-1848) — bypasses regular play pipeline. Verify: card-on-play triggers (Tingsha, Toy Ornithopter, etc.) still fire on Medical-Kit-played statuses. | **open** |
| D38 | Pure Water — naming match | not-a-bug | `packages/engine-rs/src/relics/defs/pure_water.rs:23` | `decompiled/java-src/com/megacrit/cardcrawl/relics/PureWater.java:15` | Naming/registration match. Recorded for completeness — seed used "PureWater". | **closed (not-a-bug)** |
| D39 | Mercury Hourglass — interaction with Sharp Hide via deal_damage_to_enemy | not-a-bug | `packages/engine-rs/src/engine.rs:2454-2511`, def `mercury_hourglass.rs:8` | `decompiled/java-src/com/megacrit/cardcrawl/relics/MercuryHourglass.java:31` | Java uses `DamageType.THORNS` which DOES interact with Sharp Hide. Rust path goes through `deal_damage_to_enemy` which triggers Sharp Hide retaliation (line 2503). **Match.** | **closed (not-a-bug)** |
| D40 | Shuriken — counter reset at turn start matches Java | not-a-bug | `packages/engine-rs/src/relics/defs/shuriken.rs:23-28` | `decompiled/java-src/com/megacrit/cardcrawl/relics/Shuriken.java:30-32` | 3-attack threshold per turn. Rust resets via `RESET_EFFECTS` at TurnStart. Match. | **closed (not-a-bug)** |
| D41 | Thread and Needle — match | not-a-bug | `packages/engine-rs/src/relics/defs/thread_and_needle.rs:8-19` | `decompiled/java-src/com/megacrit/cardcrawl/relics/ThreadAndNeedle.java:27-31` | 4 Plated Armor at combat start. Match. | **closed (not-a-bug)** |
| D42 | Oddly Smooth Stone — match | not-a-bug | `packages/engine-rs/src/relics/defs/oddly_smooth_stone.rs:8-19` | `decompiled/java-src/com/megacrit/cardcrawl/relics/OddlySmoothStone.java:27-31` | +1 Dexterity at combat start. Match. | **closed (not-a-bug)** |
| D43 | Meal Ticket — match | not-a-bug | `packages/engine-rs/src/run.rs:2559-2566` | `decompiled/java-src/com/megacrit/cardcrawl/relics/MealTicket.java:28-34` | Heal 15 on shop room enter via `heal_run_player`. Match. | **closed (not-a-bug)** |
| D44 | Ceramic Fish — only fires on `add_card_reward`, same root cause as D34 Frozen Egg 2 | unverified | `packages/engine-rs/src/run.rs:2154-2165` | `decompiled/java-src/com/megacrit/cardcrawl/relics/CeramicFish.java:36-38` | +9 gold per `onObtainCard` — only fires in `add_card_reward`. Events/shrines bypass it. Cross-ref D34 — fixing the unified `obtain_card` helper resolves both. | **open** |
| D45 | Runic Pyramid — match | not-a-bug | `packages/engine-rs/src/engine.rs:1359` | `decompiled/java-src/com/megacrit/cardcrawl/relics/RunicPyramid.java` (passive in DrawPile) | Wired inline. Match. | **closed (not-a-bug)** |
| D46 | Ice Cream — algebraically equivalent | not-a-bug | `packages/engine-rs/src/engine.rs:1102-1106` | `decompiled/java-src/com/megacrit/cardcrawl/relics/IceCream.java` (passive — skips loseEnergy) | Java skips end-of-turn `loseEnergy()`; Rust adds `max_energy` at turn start instead. Same end state. Match. | **closed (not-a-bug)** |
| D47 | Centennial Puzzle EffectState slot collision risk | unverified | `packages/engine-rs/src/relics/defs/centennial_puzzle.rs:16-19` | n/a | Verify no other relic on the same owner slot writes to index 0. Self-Forming Clay, Runic Cube, Emotion Chip, Red Skull all use `OnPlayerHpLoss`. If they share `EffectState`, slot collision could leak. Need to inspect `EffectState` per-owner allocation. | **open** |
| D48 | Buffer power consumed even when post-block damage is 0 | bug | `combat_hooks.rs:162-166` | `powers/BufferPower.java:41-46` | Rust short-circuits with `continue` and decrements BUFFER on every enemy hit before block calc. Java only triggers `ReducePowerAction` when `damageAmount > 0` post-block. Side effect: Rust ALSO skips Thorns/FlameBarrier retaliation on the negated hit. Fix: move Buffer check after `calculate_incoming_damage`, gate on `result.hp_loss > 0` post-block, allow Thorns to retaliate. | **open** |
| D49 | Calipers block retention math wrong (caps at 15 instead of subtracting 15) | bug | `engine.rs:1151-1159` | `actions/GameActionManager.java:351` | **Fixed in commit `d9a7c853`**. `block = (block - 15).max(0)` matches Java `loseBlock(15)`. New regression tests in `test_dead_system_cleanup_wave20.rs` for subtract-15, truncate-at-0, and preserve-high-totals scenarios. | **closed** (`d9a7c853`) |
| D50 | Vault (skip_enemy_turn) still decrements debuff stacks and Intangible | bug | `engine.rs:1545-1582` | `actions/GameActionManager.java:323-326` | **Fixed in commit `2db11718`**. Moved the `decrement_debuffs` + Intangible decrement inside the `else` branch of the skip-enemy-turn check. When Vault fires, debuffs persist into the next round. | **closed** (`2db11718`) |
| D51 | Confusion cost roll uses wrong RNG stream and rolls on play instead of draw | bug | `engine.rs:1804-1807, :1761-1764` | `powers/ConfusionPower.java:36-45` | Java applies `cardRandomRng.random(3)` in `onCardDraw` and persists rolled cost on `card.cost`. Rust rolls cost on play via generic `self.rng` (so shuffles/random-target picks/Strange Spoon perturb the roll), and uses constant-1 in legality. Symptom: card listed legal then refuses to play (rolled cost > energy); replay non-deterministic. Fix: roll on `apply_on_draw` with dedicated stream and store on CardInstance. | **open** |
| D52 | RNG streams under-partitioned: shuffle/cardRandom/misc all share `self.rng` | bug | `engine.rs:118-149`, uses at `:266, :1806, :2116, :2674, :2854, :2903, :2909` | `dungeons/AbstractDungeon.java:148-151, 393-401, 1738-1741` | Java has 6 separate `Random` instances. Rust collapsed everything except `ai_rng` into `self.rng`. Concrete effect: a Strike that shuffles + Wallop targeting + Strange Spoon flip + Confusion cost roll all advance the same stream. Java independent. Fix: split into `shuffle_rng`/`card_random_rng`/`misc_rng`; thread through call sites. Big structural follow-on to D1. | **open** |
| D53 | Per-floor RNG re-seeding missing | unverified | `engine.rs:138-161` | `dungeons/AbstractDungeon.java:1738-1741` | Java reseeds aiRng/shuffleRng/cardRandomRng/miscRng to `Settings.seed + floorNum` at every floor. Rust seeds once per `CombatEngine::new`. Training pipeline currently passes `7000 + case.floor` per `run_replay.py:259` — partial match for ai_rng but doesn't reseed shuffle/card streams. Cross-engine snapshot replay diverges. Fix: split rngs (D52) then document/compute the per-floor base. | **open** |
| D54 | Ritual fires at start of next enemy turn instead of end-of-round | bug | `powers/defs/enemy.rs:24-29` (`Trigger::EnemyTurnStart` + `NotFirstTurn`) | `powers/RitualPower.java:46-55` | Java applies Ritual strength after all enemy turns finish. Rust applies at `EnemyTurnStart` per enemy. Net effect with multiple enemies: divergent — Java applies to ALL at end-of-round boundary; Rust per-enemy independently, so cross-enemy intent damage calc could see stale STR. Also affects observation between turn boundaries. Fix: move `RITUAL_TRIGGERS` to `Trigger::EnemyEndOfRound` (add trigger if missing). Cross-ref D12. | **open** |
| D55 | Player Poison ticks at end of player turn instead of start | bug | `engine.rs:1525-1537` (end_turn) | `powers/PoisonPower.java:58-64` (`atStartOfTurn`) | Java's `PoisonPower.atStartOfTurn` runs at the start of the OWNER's turn. Player poison ticks at end of player turn in Rust. With Vault skipping enemies the timing diverges. Enemy poison correctly handled at `combat_hooks.rs:88`. Fix: move tick into `start_player_turn`. | **open** |
| D56 | Per-floor turn order: Java decrements debuffs before block decay; Rust may differ | unverified | `engine.rs:1145-1164` (block decay) vs `:1551-1572` (debuff decrements) | `actions/GameActionManager.java:325, 347-353` | Likely benign but worth confirming no Rust per-turn power reads a status value mid-window. Spot check needed: Biased Cog reads FOCUS post-decay; verify no STR/DEX read between debuff decrement and block reset. | **open** |
| D57 | Ice Cream / TempStrengthLoss parity not tested | unverified | `engine.rs:1102-1106` (Ice Cream); `:1332-1345` (TempStrengthLoss revert) | `relics/IceCream.java`; n/a (custom Rust status) | Ice Cream algebraically equivalent (D46 closed). TempStrengthLoss is custom Rust status mirroring LoseStrength on enemies — Java handles via individual cards (Siphon Soul). Verify no enemy-death path leaves stale TEMP_STRENGTH_LOSS. | **open** |
| D58 | Static Discharge channel can dispatch retaliation Lightning bypassing Slow/Flight | bug | `combat_hooks.rs:194-221` | `powers/StaticDischargePower.java:29-37`; `actions/defect/ChannelAction` | Rust channels Lightning AND if channel pushes out front orb, deals Lightning damage via raw arithmetic (`e.entity.block.min(dmg); e.entity.hp -= hp_dmg_e`). This bypasses Slow's `slow_damage_multiplier`, Flight halving, Invincible cap, Curl-Up/Malleable/Sharp Hide/Shifting on-hit triggers, AND Spore Cloud / Gremlin Horn on death. Java routes through standard `DamageAction` -> `info.applyPowers`. Fix: route through `engine.deal_damage_to_enemy()`. | **open** |
| D59 | Vulnerable/Weak/Frail decrement on enemy-applied turn (no `justApplied` flag) | bug | `engine.rs:1552-1564`; `powers/debuffs.rs:7-11` | `powers/VulnerablePower.java:32-50`, `WeakPower.java:31-50`, `FrailPower.java:29-47` | **Fixed in commit `0d4c0cbf` (2026-04-18).** Added `apply_debuff_from_enemy` + parallel `{WEAKENED,VULNERABLE,FRAIL}_JUST_APPLIED` flag statuses (StatusId 253-255). `combat_hooks.rs::execute_enemy_move` now calls the enemy-sourced variant for WEAK/VULNERABLE/FRAIL; `decrement_debuffs` clears the flag on first end-of-round and skips the decrement so the debuff lasts its full duration. Regression tests in `tests/test_debuff_timing_parity.rs` (8 cases: enemy-applied skip per-debuff-type, player-applied immediate decrement, re-stacking, Artifact/Ginger/Turnip interactions). Full lib suite 2215/2215 passing. | **closed** (commit `0d4c0cbf`) |
| D60 | Combust HP loss hardcoded to 1 (does not stack) | bug | `powers/buffs.rs:407-414` | `powers/CombustPower.java:24-29,44-48` | Java tracks `hpLoss` separately from `amount`, incrementing on every `stackPower`. So 2 Combusts = 2 HP loss + 10 dmg. Rust always returns `(1, combust_amount)`. Stacked Combust under-charges player HP cost — invisibly favors Combust strategy. Fix: track hp_loss in EffectState. | **open** |
| D61 | Player Regen decrements per turn instead of healing forever at amount | bug | `engine.rs:1518-1522` | `powers/RegenPower.java:34-38` | Java heals `amount` permanently. Rust does `add_status(REGENERATION, -1)` each turn. **Effect**: 5-stack Regen Potion heals 5+4+3+2+1=15 HP instead of 5/turn forever. Enemy-side def (`powers/defs/enemy.rs:44`) is correct (heal-only, no decrement); only player path wrong. | **open** |
| D62 | Sharp Hide triggers on damage-through-block instead of on Attack card play | bug | `engine.rs:2501-2504` | `powers/SharpHidePower.java:38-44` | Java fires on **any Attack card play**, regardless of damage hitting. Rust only fires when `hp_damage > 0`. **Effect**: blocked Attacks vs Reptile/Spike Slime/Bear/WrithingMass should still cost player N HP — Rust silently skips retaliation. | **open** |
| D63 | Curl Up triggers on non-Attack damage and on lethal blow | partial-fix | `engine.rs:2483-2505` | `powers/CurlUpPower.java:33-43` | **Lethal-blow guard landed in commit `9a337a35`** (Curl Up no longer fires when `entity.hp <= 0` post-hit). Type-NORMAL guard is implicit at the call site: `deal_damage_to_enemy` is the Normal damage path only; Thorns / HP_LOSS go through `apply_hp_loss` and bypass Curl Up entirely. Adjacent Malleable (lines 2504+) still needs the same lethal guard — separate sub-issue. | **partial-fix** (`9a337a35`; Malleable follow-up queued) |
| D64 | Anger/Enrage triggers on Powers as well as Skills | bug | `engine.rs:1953-1962` | `powers/AngerPower.java:37-42` | Java uses `card.type == SKILL`. Rust uses `card.card_type != Attack`. **Effect**: playing Powers (Inflame, Demon Form, Establishment, Battle Hymn) vs Gremlin Nob now grants him Strength like a Skill would. | **open** |
| D65 | No 999 amount cap on Strength/Dex/Focus/Energized/Plated Armor or 9999 on Poison | unverified | `powers/buffs.rs` (raw i32) | `powers/StrengthPower.java:27-32` etc. | Java clamps. Rust uses raw `i32`. Real combats almost never hit limits but mark as open pending decision. | **open** |
| D66 | Choke applies Constricted instead of ChokePower (completely different mechanic) | bug | `cards/silent/choke.rs:9-19` | `powers/ChokePower.java`; `cards/red/Choke.java:31-32` | Java applies `ChokePower` — deals 3/5 HP loss to enemy **per card played by player**, removed at start of next player turn. Rust applies `CONSTRICTED` (Snake Plant debuff: damage at end of own turn). **Major Silent damage source missing.** Fix: implement ChokePower as new status with `OnPlayerCardPlayed` trigger. | **open** |
| D67 | Enemy Intangible decrements every round (no `justApplied`) | bug | `engine.rs:1559-1562` | `powers/IntangiblePower.java:30-31,52-63` | Java skips first decrement via `justApplied`. Rust decrements unconditionally. Low impact (Nemesis re-applies each turn) but matters for player-applied Apparitions/Wraith Form/Spirit Shield-on-enemies (rare). | **open** |
| D68 | Spore Cloud applies Vulnerable on lethal blow | bug | `engine.rs:2538-2544` | `powers/SporeCloudPower.java:36-43` | Java checks `isBattleEnding()` and skips. Rust always applies. Affects post-combat observation snapshots; mostly cosmetic but could perturb solver value at terminal nodes. | **open** |
| D69 | DrawCardNextTurn fires before standard card draw | bug | `powers/buffs.rs:654` (process_start_of_turn) | `powers/DrawCardNextTurnPower.java:36-40` (`atStartOfTurnPostDraw`) | Java triggers post-draw; Rust pre-draw. Matters for Mayhem/Tools of Trade/Snecko Eye ordering. Fix: move `DrawCardNextTurn` decrement+draw into post-draw hook. | **open** |
| D70 | Equilibrium decrement timing | unverified | `powers/buffs.rs::decrement_equilibrium` (in `process_end_of_turn`) | `powers/EquilibriumPower.java::atEndOfRound` | Java `atEndOfRound`. Rust calls in `process_end_of_turn` (player end-of-turn). Need to trace: does `process_end_of_turn` actually run in production? Verify Watcher Equilibrium card timing. | **open** |
| D71 | Hex card ID "Daze" vs Java "Dazed" | unverified | `cards/status.rs` (Daze registration) | `cards/status/Dazed.java` | Both ethereal cost-2 no-op statuses; gameplay matches but training pipeline string-comparison risk if any Python code looks for "Dazed". | **open** |
| D72 | Vulnerable / Weak / Frail decrement on enemy-applied turn (no `justApplied` flag) | bug | `packages/engine-rs/src/engine.rs:1552-1564`, `packages/engine-rs/src/powers/debuffs.rs:7-11` | `decompiled/java-src/com/megacrit/cardcrawl/powers/VulnerablePower.java:32-50`, `WeakPower.java:31-50`, `FrailPower.java:29-47` | Java sets `justApplied=true` when source is monster (and `turnHasEnded` for Vuln); first `atEndOfRound` skips decrement. Rust unconditionally decrements all 3 debuffs after every enemy turn block. Effect: enemy-applied 1-stack Weak/Vuln/Frail vanishes a turn early. Fix: add per-status `*_JUST_APPLIED` flag on EntityState and skip first decrement when set. | **open** |
| D73 | Combust HP loss hardcoded to 1 regardless of stacks | bug | `packages/engine-rs/src/powers/buffs.rs:407-414` | `decompiled/java-src/com/megacrit/cardcrawl/powers/CombustPower.java:24-29,44-48` | Java `hpLoss` field starts at constructor value (typically 1) and increments on each `stackPower`. So 2 Combusts → 2 HP loss + (5+5)=10 enemy damage. Rust always returns `(1, combust_amount)`. Fix: track `sid::COMBUST_HP_LOSS` counter that increments on stack. | **open** |
| D74 | Player Regen decrements per turn instead of healing forever | bug | `packages/engine-rs/src/engine.rs:1518-1522` | `decompiled/java-src/com/megacrit/cardcrawl/powers/RegenPower.java:34-38` | Java `RegenPower` (POWER_ID "Regeneration", from Regen Potion / Bird-Faced Urn / Watcher Wreath of Flame) heals `amount` per turn forever — never decrements. Rust subtracts 1 per turn so 5-stack regen heals 5+4+3+2+1=15 instead of 5/turn forever. Fix: drop the `add_status(REGENERATION, -1)` line for player. (Enemy regen via `DEF_REGENERATION` runtime def already correctly heals without decrementing.) | **open** |
| D75 | Sharp Hide triggers on damage-through-block instead of player Attack play | bug | `packages/engine-rs/src/engine.rs:2501-2504` | `decompiled/java-src/com/megacrit/cardcrawl/powers/SharpHidePower.java:38-44` | Java fires `SharpHidePower.onUseCard` whenever player plays an Attack card (regardless of whether it lands or hits). Rust only fires when `hp_damage > 0` to that enemy in `deal_damage_to_enemy`. Effect: blocked Attacks should still cost player N HP (Reptile/Spike Slime / Bear / WrithingMass). Rust silently skips the retaliation. Fix: hook on player Attack card play emit, not on enemy damage path. | **open** |
| D76 | Curl Up triggers on non-Attack damage (poison, Thousand Cuts, Pressure Points) and on lethal blow | bug | `packages/engine-rs/src/engine.rs:2483-2489` | `decompiled/java-src/com/megacrit/cardcrawl/powers/CurlUpPower.java:33-43` | Java requires `info.type == NORMAL` AND `damageAmount < currentHealth` AND `info.owner != null`. Rust triggers on any `hp_damage > 0` in `deal_damage_to_enemy` — Thousand Cuts (THORNS damage routed via deal_damage_to_enemy) and Pressure Points HP-loss can erroneously trigger Curl Up block. Lethal-blow exemption (Java skips if `damage >= currentHealth`) also missing. Fix: pass damage type / source through to deal_damage_to_enemy and gate Curl Up on NORMAL + non-lethal. | **open** |
| D77 | Anger / Enrage (Gremlin Nob) triggers on Powers as well as Skills | bug | `packages/engine-rs/src/engine.rs:1953-1962` | `decompiled/java-src/com/megacrit/cardcrawl/powers/AngerPower.java:37-42` | Java `onUseCard` only fires when `card.type == SKILL`. Rust uses `card.card_type != Attack`, so Power cards (Inflame, Demon Form, Establishment, Battle Hymn) also enrage Nob. Effect: playing Powers vs Nob gives him Strength like a Skill would. Fix: change to `card.card_type == Skill`. | **open** |
| D78 | Strength / Dexterity / Focus / Energized / Plated Armor / Poison have no Java amount caps | unverified | `packages/engine-rs/src/state.rs::EntityState` (no clamp on add_status) | `decompiled/java-src/com/megacrit/cardcrawl/powers/StrengthPower.java:27-32,50-58`, `DexterityPower.java:24-32`, `FocusPower.java:42-50`, `EnergizedPower.java:35-37`, `PlatedArmorPower.java:36-39`, `PoisonPower.java:31-33` | Java clamps Strength/Dex/Focus/Energized to ±999, Plated Armor to 999, Poison to 9999. Rust uses raw i32 with no clamp. Real combats almost never hit these limits, so this likely won't surface unless a buggy infinite-loop exploit chains huge stacks (e.g. Spirit-Shield+Limit Break+Demon Form+Doppelganger). Likely intentional via "we don't model the cap"; mark unverified pending decision. | **open** |
| D79 | Player poison ticks at end of player turn instead of start | bug | `packages/engine-rs/src/engine.rs:1525-1537` | `decompiled/java-src/com/megacrit/cardcrawl/powers/PoisonPower.java:58-64` | Java `PoisonPower.atStartOfTurn` triggers at start of owner's turn. Enemy-side timing is correct in Rust (`combat_hooks.rs:88` runs at start-of-enemy-turn). Player-side runs in `end_player_turn` flow before enemy turns. Same total damage but timing affects ordering vs end-of-turn powers (Combust/Omega) and end-of-turn potion / Devotion / Nightmare decisions. Fix: move `tick_poison(player)` from end-of-player-turn to start-of-player-turn (after relics). | **open** |
| D80 | Choke (Silent uncommon) applies Constricted instead of ChokePower (completely different mechanic) | bug | `packages/engine-rs/src/cards/silent/choke.rs:9-19` | `decompiled/java-src/com/megacrit/cardcrawl/cards/green/Choke.java:31-32`, `decompiled/java-src/com/megacrit/cardcrawl/powers/ChokePower.java:37-41` | Java applies `ChokePower` which deals 3/5 HP loss to the enemy **per card the player plays** (`onUseCard`), removed at start of next player turn. Rust applies `CONSTRICTED` (= Snake Plant debuff: deals N damage to choked entity at end of THEIR turn — completely different mechanic). Significant Silent damage source missing. Fix: implement `ChokePower` as new sid::CHOKED with `OnUseCard` hook on the enemy side and `atStartOfTurn` removal. | **open** |
| D81 | Enemy Intangible decrements every round (no Java `justApplied`) | bug | `packages/engine-rs/src/engine.rs:1559-1562` | `decompiled/java-src/com/megacrit/cardcrawl/powers/IntangiblePower.java:30-31,52-63` | Java's enemy-side `IntangiblePower` sets `justApplied=true` in constructor; first `atEndOfTurn` skips decrement. Rust decrements unconditionally. Low impact: Nemesis re-applies Intangible every turn (`combat_hooks.rs:32-36`) so the off-by-one doesn't matter for Nemesis; only matters if player applies Apparitions / Wraith Form / Spirit Shield to an enemy. | **open** |
| D82 | Spore Cloud applies Vulnerable on the lethal blow that ends combat | bug | `packages/engine-rs/src/engine.rs:2538-2544` | `decompiled/java-src/com/megacrit/cardcrawl/powers/SporeCloudPower.java:36-43` | Java `onDeath` checks `isBattleEnding()` and skips Vulnerable application if last enemy. Rust always applies. Cosmetic for combat outcome but affects observation snapshots used by training (player gains a Vuln stack that immediately ends with combat). Fix: gate apply on living-enemy-count > 0 after this enemy dies. | **open** |
| D83 | DrawCardNextTurn fires before standard card draw, not after | bug | `packages/engine-rs/src/powers/buffs.rs:654`, `process_start_of_turn` | `decompiled/java-src/com/megacrit/cardcrawl/powers/DrawCardNextTurnPower.java:36-40` | Java triggers in `atStartOfTurnPostDraw` — after the standard 5-card draw. Rust consumes during `process_start_of_turn` which runs before draw. Hand size cap interactions differ: when hand is already full, Java skips capped cards (5+1 = capped at hand cap), Rust grants the bonus first then standard draw can't add. Same total cards in most cases but timing matters for Mayhem / Tools of Trade ordering and Snecko Eye energy draw. | **open** |
| D84 | Equilibrium decrement happens at end of player turn, not end of round | unverified | `packages/engine-rs/src/powers/mod.rs:742`, `decrement_equilibrium` | `decompiled/java-src/com/megacrit/cardcrawl/powers/EquilibriumPower.java:48-54` | Java `atEndOfRound` decrements after enemy turns. Rust calls `decrement_equilibrium` inside `process_end_of_turn` (player end-of-turn). NOTE: production engine.rs path may not actually call `process_end_of_turn` — need to trace where Equilibrium decrements in production. If decrement happens at end of round elsewhere, this is a phantom bug; if not, it decays a turn early. | **open** |
| D85 | Hex card ID is "Daze" in Rust but Java uses "Dazed" | unverified | `packages/engine-rs/src/engine.rs:1971`, `cards/mod.rs:2252` | `decompiled/java-src/com/megacrit/cardcrawl/powers/HexPower.java:36`, `Dazed.java:14` | Java card ID is "Dazed". Rust card registry uses "Daze" hardcoded. Both are ethereal cost -2 with no effect — gameplay matches. Verify run_parser / training pipeline doesn't expect the Java "Dazed" ID anywhere. If yes, bug; if no, intentional naming flatten with `mfx::DAZE`. | **open** |
| D86 | Hex amount: Rust adds N Daze cards per non-Attack play, matches Java `MakeTempCardInDrawPileAction(new Dazed(), this.amount)` | not-a-bug | `packages/engine-rs/src/engine.rs:1965-1973` | `decompiled/java-src/com/megacrit/cardcrawl/powers/HexPower.java:32-38` | Verified: Java's `MakeTempCardInDrawPileAction(Dazed, amount, true, true)` → adds `amount` Daze cards to draw pile per non-Attack card play. Rust does same loop. Match. | **closed (not-a-bug)** |
| D87 | Transient first-turn damage is `startingDeathDmg + 10` instead of `startingDeathDmg` | bug | `packages/engine-rs/src/enemies/act3.rs:173-182` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/beyond/Transient.java:51-58,72-79,111-112` | Java pre-computes a damage list `[startingDeathDmg, +10, +20, +30, +40, +50, +60]` and initial `count=0`, so the first attack uses `damage[0]` = 30 (A0) or 40 (A2+). Rust's `roll_transient` increments `ATTACK_COUNT` to 1 *before* computing `dmg = base + count*10`, so the first intent is 40 (A0) or 50 (A2+) — off by +10 every turn. Transient self-destructs at turn 5, so Java total across 4 attacks = 30+40+50+60 = 180 dmg; Rust = 40+50+60+70 = 220 dmg (+22% damage). Cross-refs D4 close-out math which assumed the Rust-side values. Fix: read current `status(ATTACK_COUNT)` (no +1), compute `dmg = base + count*10`, then set status to `count+1`. 1 function edit, ~3 LOC. Requires updating the 4 test sites that asserted on 40/50/60/70 damage steps. | **open** |
| D88 | HolyWater boss relic grants 3 copies of card "HolyWater" (5-block skill) instead of 3 Miracle (+1 Energy each) | bug | `packages/engine-rs/src/relics/defs/holy_water.rs:7-11`; card def in `cards/watcher/holywater.rs:6-13` | `decompiled/java-src/com/megacrit/cardcrawl/relics/HolyWater.java:29` | Java `MakeTempCardInHandAction(new Miracle(), 3)`. Rust adds card id `"HolyWater"` which is a different card (5-block self-target). Net: Watcher boss relic gives 15 block instead of 3 free Energy per combat. Test `tests/test_relic_runtime_wave15.rs:37-43` codifies the buggy behavior. See [relics.md R1](./audit-reports/relics.md). Fix: switch to id `"Miracle"` (the proper card already exists). | **open** |
| D89 | Fasting (`EnergyDown`) start-of-turn energy drain not applied | bug | `packages/engine-rs/src/powers/buffs.rs:663` (writes `StartOfTurnResult.extra_energy -= get_energy_down`); no consumer in `engine.rs` | `decompiled/java-src/com/megacrit/cardcrawl/powers/watcher/EnergyDownPower.java:atStartOfTurn` (`LoseEnergyAction(amount)`) | Fasting card grants Str/Dex/draw bonuses but the defining cost (-1 energy/turn) is never deducted. `process_start_of_turn` exists as helpers but is only called from unit tests. Major balance deviation for Watcher Fasting builds. See [watcher-stances-scry-mantra.md WS11](./audit-reports/watcher-stances-scry-mantra.md). | **open** |
| D90 | MalleablePower never resets per turn — accumulates indefinitely | bug | `packages/engine-rs/src/engine.rs:2521-2527` (increments +1 on every hit; no reset path) | `decompiled/java-src/com/megacrit/cardcrawl/powers/MalleablePower.java:atEndOfTurn` (resets to `basePower`) | Java resets Malleable to base each enemy end-of-turn. Rust accumulates across turns. Chosen/Stone Wyrm multi-turn fights inflate block-per-hit unboundedly. Fix: track `MALLEABLE_BASE` sid and reset on enemy end-of-turn. See [powers-buffs-debuffs.md P1](./audit-reports/powers-buffs-debuffs.md). | **open** |
| D91 | `deal_damage_to_player` (effect-DSL `Target::Player`) bypasses entire damage pipeline | bug | `packages/engine-rs/src/effects/runtime.rs:1715` routes through `engine.deal_damage_to_player` which runs only `block → player_lose_hp` | `decompiled/java-src/com/megacrit/cardcrawl/cards/AbstractCard.damage` + `DamageAction.update` | No Wrath 2x, no Vulnerable 1.5x, no Intangible cap, no Torii, no Tungsten Rod, no Plated Armor decrement, no Static Discharge trigger. Content that targets Player via effects DSL (Reaper self-damage, Hemokinesis, Reckless Charge, Feed self-hits, potion self-hits, monster `DamageEffect { target: Player }`) silently skips powers. Highest-leverage silent-drift source. See [damage-engine-flow.md DM1](./audit-reports/damage-engine-flow.md). | **open** |
| D92 | 44 orphan test files (~230 `#[test]` fns) never compiled | bug | `packages/engine-rs/src/tests/mod.rs` missing module declarations | n/a (test hygiene) | Batch-attach compile check proves the majority build cleanly (e.g. `test_orb_runtime_java_wave1` +11, `test_potion_runtime_action_path` +15, `test_played_card_instance_state` +5); minority are rotten (reference removed `DEF_STORM`, `hook_creative_ai`, private `powers::defs::complex`, private `effective_cost_inst`). Repo silently lost ~200 real tests. See [tests-types-api.md T1](./audit-reports/tests-types-api.md). | **open** |
| D93 | `combat_engine_from_snapshot` drops orb contents + never restores `combat_over`/`player_won` | bug | `packages/engine-rs/src/training_contract.rs:735-803` (line 784: `OrbSlots::new(snapshot.orb_slots)` discards contents; lines 857-858 write out `combat_over`/`player_won` but read-back path never restores them) | n/a (snapshot round-trip contract) | `CombatSnapshotV1` has no `orb_contents` field. Breaks Defect PUCT from snapshots and mid-fight terminal edges. See [tests-types-api.md T2](./audit-reports/tests-types-api.md). Fix: add orb_contents to snapshot and restore terminal flags. | **open** |
| D94 | Event pool never removes used events | bug | `packages/engine-rs/src/run.rs:2915-2924` | `decompiled/java-src/com/megacrit/cardcrawl/dungeons/AbstractDungeon.java:1936-1977` (`generateEvent` calls `eventList.remove(event)` after selection) | Same event can repeat on consecutive `?` rooms in one run. Distorts deck-shaping stats and over-exposes Colosseum / Drug Dealer J.A.X. / Nest Ritual Dagger branches. See [run-map-events-shop.md RN-EV-01](./audit-reports/run-map-events-shop.md). Fix: track used events per run in `RunState`. | **open** |
| D95 | Shrine events defined but unreachable | bug | `packages/engine-rs/src/events/shrines.rs` (17 shrine events defined); `events/mod.rs:444-446` exposes `typed_shrine_events` but it's not referenced from `enter_event`; `run.rs:2915-2924` event-gen never consults shrine pool | `decompiled/java-src/com/megacrit/cardcrawl/dungeons/AbstractDungeon.java:1855-1929` (`shrineChance` 0.25 + `shrineList` / `specialOneTimeEventList`) | 17 defined shrines (Accursed Blacksmith / Bonfire / Designer / Duplicator / Face Trader / Fountain / Knowing Skull / Lab / N'loth / SecretPortal / The Joust / WeMeetAgain / Woman in Blue / etc.) never trigger in rollouts. See [run-map-events-shop.md RN-EV-02/04](./audit-reports/run-map-events-shop.md). | **open** |
| D96 | Orb Lightning / Dark damage routed through NORMAL damage-type triggers | bug | `packages/engine-rs/src/combat_hooks.rs` orb-dispatch path; `engine.rs::deal_damage_to_enemy` | `decompiled/java-src/com/megacrit/cardcrawl/orbs/Lightning.java` + `Dark.java` use `DamageType.THORNS` | Curl Up, Malleable, Slow all fire on Lightning/Dark orb hits when Java explicitly gates them on `type == NORMAL`. Defect runs vs Louse / Sentry / Guardian / any slow-applier get biased damage math. Corollary: enemy Slow multiplier also wrongly applies to orb damage (OC12). See [other-class-cards.md OC2/OC12](./audit-reports/other-class-cards.md). Fix: add damage_type parameter to deal_damage_to_enemy and gate Curl Up / Malleable / Slow on NORMAL. | **open** |
| D97 | GremlinTsundere / GremlinSneaky does literally nothing each turn | bug | `packages/engine-rs/src/enemies/mod.rs:853` (empty match arm `/* Does nothing each turn */`) | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/GremlinTsundere.java` (Protects allies for 7/8/11 block or Bashes 6/8 if alone) | In Gremlin Gang combats this enemy is effectively non-existent. Major under-pricing of the fight. See [enemies-act1.md](./audit-reports/enemies-act1.md). | **open** |
| D98 | ApologySlime falls through to default (6-damage attack) | bug | `packages/engine-rs/src/enemies/mod.rs:903-909` (no match arm; hits default) | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/ApologySlime.java` (3-dmg + 50% Weak-1 apology) | Vanilla 6-damage attack instead of Java's apology move. See [enemies-act1.md](./audit-reports/enemies-act1.md). | **open** |
| D99 | SphericGuardian missing Barricade + Artifact 3 passives | bug | `packages/engine-rs/src/enemies/mod.rs:565-568` init only sets block 40; `enemies/act2.rs:164-176` never calls Activate scaling | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/SphericGuardian.java:77-79,92-97` (prebattle Barricade + Artifact 3 + Activate 25/35 A17+) | Rust gives only an opening block move of 40 with no Barricade, no Artifact, and no A17+ ascension scaling on Activate. See [enemies-act2-act3-act4.md](./audit-reports/enemies-act2-act3-act4.md). | **open** |
| D100 | TheCollector REVIVE (case 5) completely missing | bug | `packages/engine-rs/src/enemies/act2.rs:312-330` (`roll_collector` has no Revive branch; no minion-death hook) | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/TheCollector.java:161-169,189` | Java respawns dead TorchHeads on MOVE 5 (num ≤ 25). Combined with missing A19+ stats (strAmt=5, megaDebuffAmt=5, rakeDmg=21), the Collector fight is strictly under-scaled from A4 upward. See [enemies-act2-act3-act4.md](./audit-reports/enemies-act2-act3-act4.md). | **open** |
| D101 | CorruptHeart conflates A9 with A19 via `hp>=800` gate | bug | `packages/engine-rs/src/enemies/mod.rs:793-803` flips INVINCIBLE=200, BEAT_OF_DEATH=2 when hp≥800 | `decompiled/java-src/com/megacrit/cardcrawl/monsters/beyond/CorruptHeart.java:92-101` only flips those at `ascensionLevel>=19`, not at the A9 HP threshold | At A9-A18, Rust Heart gets an A19 passive profile it shouldn't. bloodHit/echo flip at A4+ Java; Rust conflates with HP gate. See [enemies-act2-act3-act4.md](./audit-reports/enemies-act2-act3-act4.md). | **open** |
| D102 | SnakeDagger Explode damage type wrong (attack instead of HP_LOSS) | bug | `packages/engine-rs/src/enemies/act3.rs:277-283` (plain attack damage) | `decompiled/java-src/com/megacrit/cardcrawl/monsters/beyond/SnakeDagger.java:48` (`DamageInfo(this, 25, DamageInfo.DamageType.HP_LOSS)`) | Java Explode bypasses block & Barricade. Rust goes through normal damage path. Block negates the Explode. See [enemies-act2-act3-act4.md](./audit-reports/enemies-act2-act3-act4.md). | **open** |
| D103 | VelvetChoker onEquip `+1 permanent energy` missing | bug | `packages/engine-rs/src/relics/defs/velvet_choker.rs` (no equip hook); `run.rs:2205-2228` missing match arm | `decompiled/java-src/com/megacrit/cardcrawl/relics/VelvetChoker.java:42-45` (`++player.energy.energyMaster`) | Rust gates card plays at 6/turn (correct) but never grants +1 permanent energy on equip. Cross-ref D32 (Cursed Key) same omission pattern. See [relics.md R2](./audit-reports/relics.md). | **open** |
| D104 | Philosopher's Stone onEquip `+1 permanent energy` missing | bug | `packages/engine-rs/src/relics/defs/philosophers_stone.rs:2` (stale comment claims "handled via max_energy on equip"); no such handler | `decompiled/java-src/com/megacrit/cardcrawl/relics/PhilosopherStone.java:52-54` | Only +1 STR to enemies is implemented. Owner loses the +1 permanent energy. See [relics.md R3](./audit-reports/relics.md). | **open** |
| D105 | Runic Dome completely unimplemented (+1 energy AND intent hiding both absent) | bug | not present (only string in `packages/engine-rs/src/obs.rs:59`) | `decompiled/java-src/com/megacrit/cardcrawl/relics/RunicDome.java:40-42` | Not in `relics/defs/*`, not in `engine.rs`, not in `relic_flags.rs`. See [relics.md R4](./audit-reports/relics.md). | **open** |
| D106 | DeadBranch relic entirely unimplemented | bug | not present (only comment at `packages/engine-rs/src/engine.rs:3091`) | `decompiled/java-src/com/megacrit/cardcrawl/relics/DeadBranch.java:20-27` | onExhaust: add random card to hand. Defining synergy for Watcher exhaust builds (Carve Reality, Conclude, Explosive Purge, Sands of Time). See [relics.md R7](./audit-reports/relics.md). | **open** |
| D107 | Enchiridion relic stub only | bug | `packages/engine-rs/src/relics/defs/enchiridion.rs:6` (empty `triggers: &[]`) | `decompiled/java-src/com/megacrit/cardcrawl/relics/Enchiridion.java:25-34` | atPreBattle adds random 0-cost Power card to combat-start hand. Watcher Power archetype (Adaptation, Fasting, Wallop) loses turn-1 advantage. See [relics.md R6](./audit-reports/relics.md). | **open** |
| D108 | Golden Eye relic ignored by ScryAction | bug | `packages/engine-rs/src/engine.rs:3067-3084` (`do_scry`) never checks `has_relic("GoldenEye")` | `decompiled/java-src/com/megacrit/cardcrawl/actions/utility/ScryAction.java:22-27` (+2 scry when relic present) | Golden Eye appears in `obs.rs:45` name list but nowhere in scry logic. Dead for Watcher relic builds. See [watcher-stances-scry-mantra.md WS5](./audit-reports/watcher-stances-scry-mantra.md). | **open** |
| D109 | Foresight skips `EmptyDeckShuffleAction` when draw pile is empty | bug | `packages/engine-rs/src/engine.rs:1271-1277` (direct `do_scry`; returns early when empty at `:3067-3071`) | `decompiled/java-src/com/megacrit/cardcrawl/powers/watcher/ForesightPower.java:34-40` (queues `EmptyDeckShuffleAction` before scry) | Under Runic Pyramid or heavy discard, Rust loses both the scry and the Nirvana proc. See [watcher-stances-scry-mantra.md WS4](./audit-reports/watcher-stances-scry-mantra.md). | **open** |
| D110 | `CannotChangeStancePower` never enforced | bug | `packages/engine-rs/src/engine.rs:2952-3000` (`change_stance` never consults helper at `powers/buffs.rs:492`); no turn-based decrement | `decompiled/java-src/com/megacrit/cardcrawl/actions/watcher/ChangeStanceAction.java:32-35`; `powers/watcher/CannotChangeStancePower.java` | Outsider / future Watcher-adversary mechanics locking stance changes do nothing in Rust. Latent (no current card applies). See [watcher-stances-scry-mantra.md WS3](./audit-reports/watcher-stances-scry-mantra.md). | **open** |
| D111 | Start-of-turn / post-draw conflated — pre-draw timing for DemonForm/Brutality/NoxiousFumes/ToolsOfTheTrade | bug | `packages/engine-rs/src/powers/buffs.rs::process_start_of_turn` applies all start-of-turn effects pre-draw | Java `atStartOfTurnPostDraw` (post-draw) for DemonForm/Brutality/NoxiousFumes/ToolsOfTheTrade vs `atStartOfTurn` (pre-draw) for Berserk/InfiniteBlades/NextTurnBlock/DrawCardNextTurn/Phantasmal/Bias/RechargingCore/Loop/Mayhem/Hello/Panache | Brutality draw-then-damage order wrong; NoxiousFumes Artifact off-by-one; DemonForm STR available 1 turn earlier than Java; ToolsOfTheTrade discard target set differs. Also overlaps D83 (DrawCardNextTurn). See [powers-buffs-debuffs.md P2](./audit-reports/powers-buffs-debuffs.md). Fix: split pre-draw/post-draw passes. | **open** |
| D112 | SadisticPower missing exclusions (fires on Shackled, self-debuffs, buffs) | bug | `packages/engine-rs/src/powers/debuffs.rs:215-227` (`apply_debuff_with_sadistic` fires on any applied debuff) | `decompiled/java-src/com/megacrit/cardcrawl/powers/SadisticPower.java:onApplyPower` filters `source != owner`, `type != DEBUFF`, `ID == "Shackled"`, `target == owner` | Fires on Shackled (double-dips with GainStrength recovery); fires on self-applied LoseStrength / LoseDexterity. See [powers-buffs-debuffs.md P3](./audit-reports/powers-buffs-debuffs.md). | **open** |
| D113 | Missing powers: Phantasmal / Rebound / Bias / BackAttack / Stasis / RechargingCore / TimeMaze / Amplify / Loop / Hello / Mayhem / Panache / Heatsink / Storm | bug | no sid / no def / no trigger for these 14 power IDs | `decompiled/java-src/com/megacrit/cardcrawl/powers/{Phantasmal,Rebound,Bias,BackAttack,Stasis,RechargingCore,TimeMaze,Amplify,Loop,Hello,Mayhem,Panache,Heatsink,Storm}Power.java` | Includes potion-sourced (Phantasmal via Ghost-in-a-Jar, TimeMaze via Nightmare) and relic/Defect-archetype powers. Missing = silent no-op when content fires them. See [powers-buffs-debuffs.md missing-table](./audit-reports/powers-buffs-debuffs.md). | **open** |
| D114 | CultistPotion sets RITUAL player status that never ticks | bug | `packages/engine-rs/src/potions/defs/cultist_potion.rs` (sets `sid::RITUAL` on player); no end-of-turn tick path for player Ritual | `decompiled/java-src/com/megacrit/cardcrawl/powers/RitualPower.java:atEndOfTurn` (grants STR on owner end-of-round, first-round-skip via justApplied) | Player gets `RITUAL=3` (base) but no STR is ever granted. CultistPotion is a no-op. See [potions.md PT4](./audit-reports/potions.md). | **open** |
| D115 | SneckoOil spuriously applies `CONFUSION=1` in addition to draw | bug | `packages/engine-rs/src/potions/defs/snecko_oil.rs` (adds CONFUSION + draw) | `decompiled/java-src/com/megacrit/cardcrawl/potions/SneckoOil.java` (draw + randomize hand costs; no Confusion status) | Rust applies Snecko Eye's Confusion debuff in addition to the draw. Java potion only randomizes costs in-hand, no persistent debuff. See [potions.md PT8](./audit-reports/potions.md). | **open** |
| D116 | Sacred Bark ignored for AncientPotion / BloodPotion / FruitJuice / (indirectly) FairyPotion | bug | `packages/engine-rs/src/potions/defs/{artifact_potion,blood_potion,fruit_juice}.rs` use `A::Fixed(N)` instead of `A::PotionPotency` | `decompiled/java-src/com/megacrit/cardcrawl/potions/{AncientPotion,BloodPotion,FruitJuice}.java` + `SacredBark.java` | Sacred Bark should double these potion effects; Rust silently no-ops because `effective_potency_runtime` is only hit through `A::PotionPotency`. See [potions.md PT1](./audit-reports/potions.md). Fix: convert Fixed→PotionPotency for these 3 potions. | **open** |
| D117 | A11+ potion potency scaling only present in `#[cfg(test)]` helpers | bug | `packages/engine-rs/src/potions/mod.rs` (A11+ scaling under test cfg only; production `CombatState` lacks ascension threading) | Java potion `use()` methods read `AbstractDungeon.ascensionLevel>=11` inline per potion | Production combat always reads base potency. Explicitly documented as a known gap in `potions/mod.rs` but silently diverges from Java A11+. See [potions.md PT10](./audit-reports/potions.md). Fix: thread ascension into CombatState (depends on D13). | **open** |
| D118 | Ascension scaling absent across Act 1 enemies | bug | `packages/engine-rs/src/enemies/mod.rs:404-515` + `enemies/act1.rs` hard-code A0 damage / debuff magnitudes / passive counts | Java per-enemy `getMove` reads `AbstractDungeon.ascensionLevel>=N` in every enemy | A2+/A17+/A19 tiers that change Cultist Ritual, Louse bite dmg, GremlinNob Anger, Hexaghost burn count, Guardian Sharp Hide etc. are not applied. Combined with D1 the engine is effectively stuck at A0 balance. See [enemies-act1.md](./audit-reports/enemies-act1.md). Systemic ~50-LOC touch per act. | **open** |
| D119 | Every module in `lib.rs` is `pub mod` — leaks internals to Python wheel | tech-debt | `packages/engine-rs/src/lib.rs:11-37` (27 public module declarations: `gameplay`, `effects`, `powers`, `potions`, `relics`, etc.) | n/a (Rust API hygiene) | Most should be `pub(crate)`. Enables accidental downstream dependencies on internals and bloats the wheel's apparent public surface. See [tests-types-api.md T12](./audit-reports/tests-types-api.md). | **open** |
| D120 | PyO3 dicts flow through `serialize_to_py_dict` JSON roundtrip with zero shape tests | bug | `packages/engine-rs/src/training_contract.rs::serialize_to_py_dict` + `packages/training/` consumers | n/a (training contract) | Any schema drift silently reshapes the Python dict. No snapshot-vs-dict round-trip test exists. See [tests-types-api.md T5](./audit-reports/tests-types-api.md). | **open** |
| D121 | 186 clippy warnings (72 auto-fixable) | tech-debt | `packages/engine-rs/` (whole crate) | n/a | Top buckets: 81 empty-line-after-doc-comment, 33 useless-PyErr-conversion in `lib.rs`, 19 if-same-branches, 14 needless-borrow. Easy `cargo clippy --fix` pass. See [tests-types-api.md T4](./audit-reports/tests-types-api.md). | **open** |
| D122 | Crippling Cloud Rust id vs Java `Crippling Poison` ID | bug | `packages/engine-rs/src/cards/silent/*.rs` registers `Crippling Cloud` | `decompiled/java-src/com/megacrit/cardcrawl/cards/green/CripplingPoison.java` (display name "Crippling Cloud", card ID "Crippling Poison") | Training-pipeline ID mismatch risk if any Python code matches on Java card ID. See [other-class-cards.md OC1](./audit-reports/other-class-cards.md). | **open** |
| D123 | Deva Form escalation formula wrong when stacked (2+ copies) | bug | `packages/engine-rs/src/powers/defs/turn_start.rs:227-244`, `packages/engine-rs/src/cards/watcher/devaform.rs:9-10` | `decompiled/java-src/com/megacrit/cardcrawl/powers/watcher/DevaPower.java:31-47` | Java `onEnergyRecharge`: grants `energyGainAmount`, then `energyGainAmount += amount` (stacks). 2 casts (amount=2) yield 2,4,6,8. Rust `GainEnergy(StatusValue(DEVA_FORM)) + AddStatus(DEVA_FORM, Fixed(1))` yields 2,3,4,5 for 2 stacks. Single-cast correct (1,2,3,4). Fix: scale AddStatus by current DEVA_FORM stacks. See [watcher-cards.md W2](./audit-reports/watcher-cards.md). | **open** |
| D124 | Pressure Points / TriggerMarks bypasses HP-loss pipeline | bug | `packages/engine-rs/src/effects/interpreter.rs:621-629`, `packages/engine-rs/src/cards/watcher/pressurepoints.rs` | `decompiled/java-src/com/megacrit/cardcrawl/powers/watcher/MarkPower.java:34-39` (uses `LoseHPAction(owner, null, amount, FIRE)`) | Java routes Mark damage through `LoseHPAction` — goes through Buffer, Intangible (caps to 1), on-loseHP hooks (Blue Candle, Centennial Puzzle), death-trigger pipeline, and `total_damage_dealt` counters. Rust subtracts `mark` directly from `enemy.entity.hp`: ignores enemy Intangible, skips on-loseHP side-effects (Spore Cloud Weak, Gremlin Horn energy), skips death counters. Broader family of D91 (effect-DSL damage bypass). See [watcher-cards.md W3](./audit-reports/watcher-cards.md). | **open** |
| D125 | Swivel `NEXT_ATTACK_FREE` flag non-stacking (overwrite to 1) | bug | `packages/engine-rs/src/effects/interpreter.rs:2092-2094` (`set_status(NEXT_ATTACK_FREE, 1)`) | `decompiled/java-src/com/megacrit/cardcrawl/powers/watcher/FreeAttackPower.java:19-43` + `decompiled/java-src/com/megacrit/cardcrawl/cards/purple/Swivel.java:30` (`ApplyPowerAction(… new FreeAttackPower(p, 1), 1)`) | Java stacks `amount` on same power ID; 2 Swivels = next 2 attacks free. Rust overwrites via `set_status` — only 1 attack freed regardless of copies. Fix: `add_status(NEXT_ATTACK_FREE, 1)` or explicit counter bump. See [watcher-cards.md W7](./audit-reports/watcher-cards.md). | **open** |
| D126 | Miracle missing `selfRetain` — tempCard Miracles discard at end of turn | bug | `packages/engine-rs/src/cards/watcher/miracle.rs:3-21`, `packages/engine-rs/src/cards/runtime_meta.rs:113-141` (retain list omits `"Miracle"`/`"Miracle+"`) | `decompiled/java-src/com/megacrit/cardcrawl/cards/tempCards/Miracle.java:29` (`this.selfRetain = true;`) | Java Miracle retains at end of turn. Rust Miracle gets discarded. Critical for Collect and Omniscience flows where cached Miracles are expected to survive end-of-turn. Fix: add `"Miracle"`/`"Miracle+"` to retain list. See [watcher-cards.md W10](./audit-reports/watcher-cards.md). | **open** |
| D127 | Collect+ spawns non-upgraded Miracles | bug | `packages/engine-rs/src/engine.rs:1226-1230` (`add_temp_cards_to_hand("Miracle", miracles)`), `packages/engine-rs/src/cards/watcher/collect.rs` | `decompiled/java-src/com/megacrit/cardcrawl/powers/watcher/CollectPower.java::onEnergyRecharge` (`Miracle card = new Miracle(); card.upgrade();`) | Java CollectPower always upgrades the generated Miracles (each gives 2 energy). Rust spawns literal ID `"Miracle"` — base 1-energy Miracles regardless of Collect vs Collect+. Compounds with D126 (retain missing). Fix: spawn `"Miracle+"`. See [watcher-cards.md W11](./audit-reports/watcher-cards.md). | **open** |
| D128 | Signature Move `canUse` excludes duplicate copies of itself | bug | `packages/engine-rs/src/effects/card_runtime.rs:15-23` (filter `candidate.def_id != card_inst.def_id`) | `decompiled/java-src/com/megacrit/cardcrawl/cards/purple/SignatureMove.java:46-57` (Java `c == this` reference equality) | Two SignatureMoves in hand each see the other as "not an attack to worry about" → both playable. Java reference-equality check blocks the second. Edge case (requires 2 copies via Havoc/Dual Wield/Nilry's/Armaments+), but real divergence. Fix: iterate with instance-ID or skip exactly ONE matching-def_id occurrence. See [watcher-cards.md W5](./audit-reports/watcher-cards.md). | **open** |
| D129 | Jaw Worm BELLOW/THRASH branches swapped in Rust dispatcher | bug | `packages/engine-rs/src/enemies/act1.rs:11-28` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/JawWorm.java:146-181` | Pre-existing from commit `9413130f` (Stage A RNG rewrite). Java defaults: 0-24 CHOMP / 25-54 THRASH / 55-99 BELLOW. Rust was emitting BELLOW in 25-54 and THRASH in >=55. Surfaced during Stage F 3-agent review of Stage D Jaw Worm changes. Fix rewrote the dispatcher to match Java's three branches with explicit sub-roll fallbacks (dominant branch). Tests updated across `test_ai_rng_parity.rs`, `test_enemy_ai.rs`, `test_enemies.rs`, `mod.rs`. | **closed** (Stage F, 2026-04-21) |
| D130 | Collector MegaDebuff trigger off-by-one (fires on turn 5 instead of turn 4) | bug | `packages/engine-rs/src/enemies/act2.rs:485` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/TheCollector.java:185` | Pre-existing `turns == 4` gate (Stage D wrapped it with `!ult_used` but the count was already wrong). Java fires `turnsTaken >= 3 && !ultUsed`, queuing MegaDebuff for turn 4. Rust was queuing for turn 5. Fix: `turns >= 3` — matches Java's post-increment semantics because `move_history.push(move_id)` at the top of `roll_next_move_with_num` is the Rust analogue of Java's `++turnsTaken` before the `RollMoveAction`. Tests `collector_a2_scaling_matches_java_expectations`, `test_collector_boss_pattern`, and `test_collector_buff_branch` rewritten to match Java parity. | **closed** (Stage F, 2026-04-21) |
| D131 | Jaw Worm sub-roll 2nd RNG draw deferred (aiRng.randomBoolean on guard trigger) | deferred | `packages/engine-rs/src/enemies/act1.rs:11-50` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/JawWorm.java:152-181` | Java's three branches each read a second `aiRng.randomBoolean(p)` when the anti-repeat guard hits: num<25+lastMove(CHOMP) splits 56.25/43.75 BELLOW/THRASH; num<55+lastTwoMoves(THRASH) splits 35.7/64.3 CHOMP/BELLOW; else+lastMove(BELLOW) splits 41.6/58.4 CHOMP/THRASH. Rust's `roll_next_move_with_num(num)` API exposes only one `num` so the sub-roll is impossible to model exactly. Fallback picks the dominant branch (noted in dispatcher comments). Revisit condition: once `roll_next_move` threads the `aiRng` reference into per-enemy dispatchers (design change), we can thread the second `random(99)` draw and replay Java's `randomBoolean(p)` exactly. | **deferred** (revisit when aiRng threading is plumbed through dispatchers) |
| D132 | Byrd grounded-state synthesis emits Fly Up instead of Java's unconditional Headbutt | bug | `packages/engine-rs/src/enemies/act2.rs:61-103` (`roll_byrd` grounded branch) | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/Byrd.java:214-215` | Pre-existing divergence surfaced in Stage F 3-agent review. When Byrd is grounded (FLIGHT=0 from Ground card / Stasis etc.), Java unconditionally emits byte 5 HEADBUTT. Rust synthesizes `BYRD_FLY_UP` + re-sets FLIGHT=3, then emits HEADBUTT only if last move was STUNNED. Under the ground-and-attack Silent strategy (Dagger Throw / Flechettes / Noxious Fumes), Rust Byrds clear their grounded state one turn later than Java — favors Silent damage and hurts solver accuracy. Fix scope ~15 LOC: replace grounded branch with single HEADBUTT set_move. Not in Stage F scope — tracked as a follow-up. | **open** |

## Stage G audit fleet — 2026-04-21

Four Opus-4.7 audit subagents (Act 1, Act 2, Act 3+4, test consolidation) ran in parallel after Stage F landed. **24 new AI-dispatcher parity bugs + 1 tautology + 130 redundant tests** surfaced across ~2.5k LOC of subagent reports. Promoted to register rows D133-D159 below; only the `|| true` tautology (D158) is fixed in Stage G. The remaining 23 code-fix rows are tracked for follow-up PRs with per-row priority markers — fixing them inline would have ballooned this PR ~10x. Priority bands:

- **P0 (Watcher A0 training-critical)**: D143 (Heart slot 0 determinism), D144 (Heart A4-A8 scaling), D140 (WrithingMass Reactive un-wired), D152 (GremlinWizard 2-turn cycle), D154 (Lagavulin 1:1 alternation), D155 (Sentry BOLT/BEAM swap). All directly bend Act 3 / boss combat math.
- **P1 (common A0 enemies)**: D146 (FungiBeast ignores num), D147/D148 (SpikeSlime_M/L ignore num), D141 (Transient damage direction), D142 (Exploder skips UNKNOWN turn), D133 (BronzeAutomaton HyperBeam early), D136 (GremlinLeader ignores aliveCount).
- **P2 (edge / high-ascension)**: everything else.

| ID | Title | Category | Rust ref | Java ref | Notes / fix scope | Status |
|---:|-------|----------|----------|----------|-------------------|--------|
| D133 | BronzeAutomaton HyperBeam fires turn 5 instead of turn 6 | bug | `packages/engine-rs/src/enemies/act2.rs:342-344` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/BronzeAutomaton.java:95-130` | Rust gates HyperBeam on `move_history.len() >= 4` which counts SPAWN (turn 1). Java's `numTurns` counter only increments inside FLAIL/BOOST branches — HyperBeam fires after **two** Flail/Boost pairs (turn 6), not after one (turn 5). Tests `bronze_automaton_cycle_matches_java_base_pattern` (test_bosses.rs:253-271) and `act2_patterns_match_java` (test_enemy_ai.rs:570-578) encode the wrong sequence and must update with the fix. Priority P1. Fix scope ~10 LOC + 2 test updates. | **open** |
| D134 | BanditLeader re-emits MOCK every 3rd turn | bug | `packages/engine-rs/src/enemies/act2.rs:319-329` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/BanditLeader.java` | Rust uses a 3-state fallthrough cycle `MOCK → AGONIZING_SLASH → CROSS_SLASH → MOCK...`. Java's `getMove()` always emits MOCK and `takeTurn()` chains `SetMoveAction(AGONIZING_SLASH)` then `SetMoveAction(CROSS_SLASH)` — MOCK only re-enters via explicit reset, which the base pattern never does. Rust thus gives the player a "free" no-damage turn every 3rd roll. Priority P2 (Bandit trio is rare in training). Fix scope: replace fallthrough with state-machine tracking the `SetMoveAction` chain. | **open** |
| D135 | BanditBear re-applies BEAR_HUG every 3rd turn | bug | `packages/engine-rs/src/enemies/act2.rs:307-317` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/BanditBear.java` | Same takeTurn-chain / roll-fallthrough mismatch as D134. Java's BEAR_HUG applies a one-shot Dex-down; Rust's fallthrough re-applies it every 3rd turn, stacking Dex penalties. Priority P2. Fix scope ties to D134 (same structural fix). | **open** |
| D136 | GremlinLeader elite ignores aliveGremlins signal | bug | `packages/engine-rs/src/enemies/act2.rs:257-268` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/GremlinLeader.java:142-232` | Java branches three ways on `numAliveGremlins`: 0 → mostly Rally-to-spawn; 1 → Encourage/Stab only (never Rally); 2 → mixed. Rust cycles `Rally → Encourage → Stab` unconditionally, ignoring ally state. Rally spawns are also not threaded to engine. Priority P1 (common A2 elite). Fix scope ~30 LOC + thread `alive_gremlin_count` through dispatcher signature + engine wire-up for Rally's SpawnMonster. | **open** |
| D137 | Champ Phase-2 Anger threshold off-by-one (`<=` instead of Java `<`) | bug | `packages/engine-rs/src/enemies/act2.rs:389` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/Champ.java` | Rust triggers Phase-2 Anger at `hp <= max_hp/2`; Java uses strict `hp < max_hp/2`. At exactly half HP, Rust enters Phase 2 one turn early. Priority P2 (single-turn skew, rare exact-half HP). Fix scope 1 LOC + boundary test. | **open** |
| D138 | Champ Defensive Stance missing `forgeTimes < 2` cap | bug | `packages/engine-rs/src/enemies/act2.rs:438` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/Champ.java` | Rust allows Defensive Stance to re-fire any time `num <= 15 && !last_move(DEFENSIVE)`. Java caps at `forgeTimes < 2` (A0..A18), so Champ can only block-and-buff twice in Phase 1. Rust gives more block + more Metallicize buildup. Priority P2 (Champ is rare A2 boss). Fix scope ~5 LOC (`sid::FORGE_TIMES` tracker + gate). | **open** |
| D139 | Snecko collapses Java two-threshold num branch to single `num<50` | bug | `packages/engine-rs/src/enemies/act2.rs:290-305` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/Snecko.java` | Java: `num<40 → TAIL / num<50 → BITE / else TAIL` (with anti-repeat). Rust: `num<50 && !lastTwoMoves(BITE) → BITE / else TAIL`. Rust over-emits BITE on the `num<40` slice (10% of rolls). Priority P2. Fix scope 3 LOC. | **open** |
| D140 | WrithingMass ReactivePower never wired into combat hooks | bug | `packages/engine-rs/src/enemies/act3.rs:186-215` (orphan `writhing_mass_reactive_reroll`) | `decompiled/java-src/com/megacrit/cardcrawl/monsters/beyond/WrithingMass.java` + `powers/ReactivePower.java` | Rust exposes a well-formed `writhing_mass_reactive_reroll` helper but never calls it from `combat_hooks.rs::on_enemy_damaged`. Java's ReactivePower re-rolls `getMove()` on every damage instance, so WrithingMass changes intent when hit. Rust WrithingMass is static once rolled — makes the fight far easier than Java. Only the unit test at `test_enemy_ai.rs:717` calls the helper. Priority P0 (affects Act 3 combat math at every instance of WrithingMass). Fix scope ~20 LOC in combat_hooks + invoke on damage taken (precedent at combat_hooks.rs:239-253 for Thorns/FlameBarrier). | **open** |
| D141 | Transient ATTACK damage direction reversed (escalates instead of matching Fading count) | bug | `packages/engine-rs/src/enemies/act3.rs:272-281` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/beyond/Transient.java:111-113` + `powers/FadingPower.java` | Java sets `startingDeathDmg + count*10` where `count` is FadingPower's remaining-turns counter starting at 5 and decrementing — damage sequence 80/70/60/50/40. Rust tracks its own `ATTACK_COUNT` starting at 0 and increments — produces 40/50/60/70/80 (reverse). Test `test_enemy_ai.rs:751-757` asserts the Rust ascending formula and thus encodes the bug as parity. Priority P1 (Transient is common A3 enemy). Fix scope ~10 LOC: read from FadingPower count instead of own counter + update test. | **open** |
| D142 | Exploder skips Java's UNKNOWN turn-2 intent | bug | `packages/engine-rs/src/enemies/act3.rs:100-114` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/beyond/Exploder.java:85-92` + `powers/ExplosivePower.java` | Java sequence: turn 1 ATTACK(9), turn 2 UNKNOWN (no visible damage, ExplosivePower counting), turn 3 EXPLODE(30). Rust skips the UNKNOWN turn: ATTACK(9) → EXPLODE(30) in 2 turns. Gives the player one less prep turn than Java. Test `test_enemy_ai.rs:699-705` encodes the Rust sequence. Priority P1 (Act 3 combats are tight). Fix scope ~10 LOC: emit `EXPLODER_UNKNOWN` on turn 2, detonate on turn 3. | **open** |
| D143 | CorruptHeart slot 0 deterministic Blood Shots (should be Java 50/50 Blood Shots / Echo) | bug | `packages/engine-rs/src/enemies/act4.rs:124-128` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/ending/CorruptHeart.java:171-199` | Java slot 0 (`moveCount % 3 == 0`): `randomBoolean() → BLOOD_SHOTS else ECHO`. Rust deterministically picks BLOOD_SHOTS. Java can emit Echo on slot 0; Rust can only emit Echo via slot 1's anti-repeat. MCTS on Heart sees a wrong branching distribution for half of slot-0 visits. Tests `test_bosses.rs:537-552` and `mod.rs:1506-1553` encode the Rust determinism. Priority P0 (Watcher A0 final boss). Fix scope ~5 LOC: branch on `num < 50` + update 2 tests. | **open** |
| D144 | CorruptHeart A4-A8 damage scaling window wrong (gated on A9+ HP threshold) | bug | `packages/engine-rs/src/enemies/mod.rs:785-803` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/ending/CorruptHeart.java` | Java splits scaling: HP bump at A9+ (max_hp 800), damage bump at A4+ (bloodHitCount 12→15, echoDmg 40→45). Rust conflates both onto `hp >= 800` — so A4-A8 Hearts (HP 750, but damage should already be A4+) use A0 damage numbers. Same A4/A9 conflation patterns at Donu/Deca (`mod.rs:744-756`) and TimeEater (`mod.rs:757-765`). Priority P0 for Heart; P2 for other bosses. Fix scope ~15 LOC per boss: thread explicit ascension signal through `create_enemy` instead of indirecting via HP. | **open** |
| D145 | Darkling turn-1 intent is 100% NIP (Java 50/50 Harden/Nip via aiRng) | bug | `packages/engine-rs/src/enemies/act3.rs:11-51` + `packages/engine-rs/src/enemies/mod.rs:647` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/beyond/Darkling.java:142-181` | Java first-move branch: `num<50 → HARDEN(byte 2)` else `NIP(byte 3)`. Rust's `create_enemy` hardcodes NIP and the dispatcher has no firstMove branch. Test `test_enemy_ai.rs:594-599` uses `expect_one_of(HARDEN\|NIP)` which trivially passes because init forces NIP — false green. Also missing: Java's `monstersIndex % 2 == 0` gate on the Chomp branch (only matters for paired Darkling encounters). Priority P2 (rare Act 3 encounter). Fix scope ~15 LOC. | **open** |
| D146 | FungiBeast dispatcher discards `num` entirely | bug | `packages/engine-rs/src/enemies/act1.rs:58-67` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/FungiBeast.java:99-111` | Java: `num<60 → BITE-preferring branch (attack) / num>=60 → GROW-preferring branch (buff)`. Rust signature `roll_fungi_beast(enemy, _num)` discards num and emits deterministic "always BITE until lastTwoMoves(BITE), then GROW". Result: FungiBeast never GROWs on turn 2 in Rust (40% of turn-2 rolls in Java give GROW). Tests `fb_no_three_bites`, `fb_after_grow_bite` encode the deterministic Rust shape. Priority P1 (FungiBeast is common A1 enemy). Fix scope ~8 LOC + update 3 tests. | **open** |
| D147 | SpikeSlime_M dispatcher discards `num` entirely | bug | `packages/engine-rs/src/enemies/act1.rs:221-230` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/SpikeSlime_M.java:105-115` | Java: `num<30 → FLAME_TACKLE-preferring / num>=30 → FRAIL_LICK-preferring` (each with anti-repeat fallthroughs). Rust signature `roll_spike_slime_m(enemy, _num)` discards num and emits TACKLE/TACKLE/LICK/TACKLE/TACKLE/LICK... Rust's cycle skews heavily toward TACKLE (damage-heavy); Java's cycle skews toward LICK (Frail debuff). Test `spike_m_no_three_tackles` encodes Rust determinism. Priority P1. Fix scope ~8 LOC + 1 test update. | **open** |
| D148 | SpikeSlime_L dispatcher discards `num` entirely | bug | `packages/engine-rs/src/enemies/act1.rs:232-241` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/SpikeSlime_L.java` | Same shape as D147 for the large variant. Test `spike_l_frail_2` encodes Rust determinism. Priority P1 (SpikeSlime_L is common A1 elite-path enemy). Fix scope ~8 LOC + 1 test update. | **open** |
| D149 | AcidSlime_S threshold `num<40` vs Java `num<30` + fabricated anti-repeat guard | bug | `packages/engine-rs/src/enemies/act1.rs:155-171` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/AcidSlime_S.java:115-121` | Java: `num<30 → TACKLE / else LICK`, no anti-repeat. Rust: `num<40 → TACKLE / else LICK` plus `else last_move(LICK) → TACKLE` fabricated anti-repeat guard. Test `acid_s_alternates` uses `num=60` so both implementations agree — silent gap. Priority P2. Fix scope ~5 LOC. | **open** |
| D150 | Louse (Red + Green) anti-repeat is single-move guard vs Java double (`lastTwoMoves`) | bug | `packages/engine-rs/src/enemies/act1.rs:69-107` (both variants) | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/LouseNormal.java` + `LouseDefensive.java` | Java first guard: `!lastTwoMoves(GROW) → GROW` so Louse can still GROW a third time if num<25. Rust uses `!last_move(GROW)` (single) — blocks the third GROW. Similar for Green Louse's SPIT_WEB. Low incidence (requires 2 GROWs in a row first). Priority P2. Fix scope ~4 LOC. | **open** |
| D151 | SlaverBlue extra `!last_move(STAB)` guard Java does not have | bug | `packages/engine-rs/src/enemies/act1.rs:115-119` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/SlaverBlue.java:111-114` | Java: `num<40 && !lastTwoMoves(STAB) → STAB`. Rust: `num<40 && !last_move(STAB) && !last_two_moves(STAB) → STAB`. After exactly one STAB in history, Java STABs again on `num<40`; Rust emits RAKE. Priority P2 (minor distribution drift). Fix scope 1 LOC. | **open** |
| D152 | GremlinWizard cycle collapsed from Java 3-turn to Rust 2-turn (~33% more damage/turn) | bug | `packages/engine-rs/src/enemies/act1.rs:307-315` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/GremlinWizard.java:142-144` | Java: `getMove()` always sets CHARGE; `takeTurn()` increments `currentCharge` (init 1); only at `currentCharge == 3` does takeTurn directly `setMove(ATTACK)`. Full cycle: CHARGE, CHARGE, ATTACK (3 turns). Rust alternates `if last_move(PROTECT) → ATTACK else PROTECT` — 2-turn cycle ATTACK every other turn. Wizard deals ~33% more damage per turn in Rust. Priority P0 (encountered often in A1 elite fights; Watcher A0 training-path). Fix scope ~15 LOC: track `currentCharge` status + wire setMove cadence. | **open** |
| D153 | GremlinTsundere dispatcher arm is empty (helper exists but not wired) | bug | `packages/engine-rs/src/enemies/mod.rs:853` (empty match arm for `GremlinTsundere\|GremlinSneaky`) + helper at `packages/engine-rs/src/enemies/act1.rs::roll_gremlin_tsundere` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/GremlinTsundere.java:122-124` | Java: `getMove` always sets PROTECT; `takeTurn` decides by `aliveCount` (>1 queues PROTECT, solo queues BASH). Rust's dispatcher arm is empty (`/* Does nothing each turn */`), so Tsundere never changes intent from its initial PROTECT — solo Tsundere stays on PROTECT forever instead of switching to BASH. The correct helper `roll_gremlin_tsundere(enemy, num, has_other_allies)` exists but is only called from the unit test. Priority P2. Fix scope ~8 LOC: thread alive-ally count through dispatcher signature OR compute from enemy list at call site. Also needed for D136 (GremlinLeader). | **open** |
| D154 | Lagavulin alternation 1:1 vs Java 2:1 attack:debuff ratio | bug | `packages/engine-rs/src/enemies/act1.rs:341-362` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/Lagavulin.java:209-223` | Java: `debuffTurnCount<2 → ATTACK else DEBUFF (reset)` — cycle ATTACK, ATTACK, SIPHON repeating (2:1 ratio). Rust: `if last_move(ATTACK) → SIPHON else ATTACK` — strict alternation (1:1 ratio). Result: Lagavulin deals ~30% less damage in Rust than Java. No existing test pins the 2:1 ratio so this drift passes current tests silently. Priority P0 (Lagavulin is a common A1 elite, key Watcher A0 combat). Fix scope ~12 LOC: track `debuffTurnCount` status. | **open** |
| D155 | Sentry BOLT and BEAM move semantics swapped | bug | `packages/engine-rs/src/enemies/act1.rs:371-378` + move-id constants | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/Sentry.java:82-108` | Java case 3 (BOLT) adds Dazed cards with **no damage**; case 4 (BEAM) deals 9 damage with **no debuff**. Rust has BOLT as a 9-damage attack and BEAM as a 9-damage attack + 2 Daze — the two moves have been collapsed into both-attack variants. Tests `sentry_alternates_bolt_beam` + `sentry_beam_damage` encode the Rust wrong shape. Priority P0 (Sentry is a common A1 encounter and elite; Watcher A0 combat impact). Fix scope ~15 LOC: remove damage from BOLT, remove debuff from BEAM, update 2 tests + mod.rs test. | **open** |
| D156 | Sentry first-move positional logic missing (`lastIndexOf(this) % 2`) | bug | `packages/engine-rs/src/enemies/mod.rs::create_enemy("Sentry")` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/Sentry.java` | Java first move: index 0/2 → BOLT, index 1 → BEAM. Rust `create_enemy` always seeds BOLT regardless of position. Paired Sentry encounter starts both with BOLT instead of BOLT/BEAM split. Priority P2. Fix scope ~8 LOC: pass monster index to `create_enemy`. | **open** |
| D157 | Looter AI RNG stream position off-by-one vs Java | bug | `packages/engine-rs/src/enemies/act1.rs:243-267` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/exordium/Looter.java` | Java's `getMove()` always returns MUG (no RNG draw); the turn-2 50/50 Smoke Bomb / Lunge split is decided in `takeTurn` (one RNG draw). Rust `roll_looter(num)` consumes one num in `roll_next_move` on every call, then uses it for both intent selection and the turn-2 split. Net: stream position is off by 1 per Looter turn 1 vs Java. Downstream ally enemies then see a different num. Priority P2. Fix scope ~10 LOC: match Java's stream pattern (don't consume on turn 1, consume on turn 2). | **open** |
| D158 | test_enemies.rs heart_has_invincible tautology (`\|\| true` defeats assertion) | bug | `packages/engine-rs/src/tests/test_enemies.rs:603-607` | n/a | `assert!(e.entity.status(sid::INVINCIBLE) > 0 \|\| e.entity.status(sid::BEAT_OF_DEATH) > 0 \|\| true);` — the `\|\| true` clause makes the assertion always pass regardless of state. Fixing Stage G by removing the `\|\| true` and dropping Heart's separate INVINCIBLE assertion (it's covered in `test_bosses.rs::corrupt_heart_initial_invincible_matches_java` at L509-525). | **closed** (Stage G, 2026-04-21) |
| D159 | test_enemies.rs 647 LOC is fully superseded by tests/mod.rs + test_enemy_ai.rs + test_bosses.rs + test_ai_rng_parity.rs | consolidation | `packages/engine-rs/src/tests/test_enemies.rs` (entire file, 83 tests) | n/a | Stage G consolidation audit surfaced: every test in this file duplicates either the per-enemy branch-coverage tests in `enemies/mod.rs` Stage D section, the per-act rollup in `test_enemy_ai.rs`, or the boss tests in `test_bosses.rs`. Plus ~45 duplicate tests in `enemies/mod.rs:946-1693` (the pre-Stage-D block). Estimated net removal: -700 LOC, -45 tests from mod.rs + 647 LOC, 83 tests from test_enemies.rs = ~35% LOC reduction, zero branch-coverage loss. Not in Stage G scope (risk of hiding coverage if done hastily) — tracked for dedicated consolidation PR. Priority P1 (reduces future audit surface + protects against more "wrong tests encoding bugs" like D129/D130 were). | **deferred** (dedicated consolidation PR) |
| D160 | BookOfStabbing A18+ stabCount increment on BigStab path missing | bug | `packages/engine-rs/src/enemies/act2.rs:233-255` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/BookOfStabbing.java:128-146` | Java: BigStab branches at L128-146 include `if (ascensionLevel >= 18) ++stabCount` alongside the regular Stab branches. Rust BigStab branches at L246, L249 set intent + emit damage but never bump `sid::STAB_COUNT`. At A18+ the subsequent Stab deals `(count+1)*6` in Java but stays at the prior counter in Rust — systematic damage underflow on A18+ Book of Stabbing. Priority P2 (mid-A1 uncommon; A18+ only). Fix scope ~4 LOC: add `bump_status(sid::STAB_COUNT, 1)` under the A18+ guard in both BigStab branches. Stage H pre-merge audit (enemy-ai scratch, 2026-04-21). | **open** |
| D161 | Mugger turn-2 deterministic BigSwipe + missing A17+ escape-block +6 + fabricated SmokeBomb→Escape chain | bug | `packages/engine-rs/src/enemies/act2.rs:44-59` | `decompiled/java-src/com/megacrit/cardcrawl/monsters/city/Mugger.java:86-114,133-136` | Java's `getMove` only ever sets byte 1 (MUG); the cycle is driven by `takeTurn` `SetMoveAction` chains. Turn 2: `aiRng.randomBoolean(0.5f)` → escape-block branch (byte 2 DEFEND, applies `escapeDef + 6` at A17+) ELSE BigSwipe. Rust deterministically emits `MUGGER_BIG_SWIPE` on turn 2, a fabricated `MUGGER_SMOKE_BOMB` on turn 3, and `MUGGER_ESCAPE` on turn 4 — none of this 3-move chain exists in Java. Rust deals ~50% more turn-2 damage vs Java EV and is missing the A17+ +6 block bump (17 total vs Rust's fixed 11). Priority P2 (Mugger is uncommon elite encounter; A17+ impact). Fix scope ~25 LOC: add 50/50 turn-2 branch, add A17+ block scaling, collapse SmokeBomb→Escape into Java's escape-block intent. Similar structural class to D134/D135. Stage H pre-merge audit (enemy-ai scratch, 2026-04-21). | **open** |
| D162 | CombatSnapshotV1 drops ai_rng state (merge-blocker F1) | bug | `packages/engine-rs/src/training_contract.rs:523-555,658-728,735-803` | n/a (training-contract determinism invariant) | `CombatSnapshotV1` serializes `rng_seed0/seed1/counter` for the combat RNG but has no field for `ai_rng`. `combat_engine_from_snapshot` rebuilds `engine.ai_rng` from defaults, so any snapshot→resume discards the AI RNG stream position. Downstream: two MCTS evaluations of equivalent snapshots at different search depths produce divergent enemy intents → transposition table corruption + training-rollout nondeterminism. P0 merge-blocker. Fix scope ~20 LOC: add `ai_rng_seed0: u64, ai_rng_seed1: u64, ai_rng_counter: i32` fields; populate from `engine.ai_rng.state_tuple()` (already serializable at `seed.rs:68-70`); restore via `engine.ai_rng.set_state_tuple(...)` in roundtrip reconstructor. Stage H pre-merge audit (training-infra scratch, 2026-04-21). | **open** |
| D163 | EnemySnapshotV1 drops move_history + AI memory (merge-blocker F2) | bug | `packages/engine-rs/src/training_contract.rs:506-521,735-803` | n/a (training-contract determinism invariant) | `EnemySnapshotV1` has fields for `id/hp/max_hp/block/move_id/move_amount/move_hits/statuses` but does NOT serialize `move_history: Vec<i32>` (`state.rs:153`). Snapshot roundtrip reconstructs enemies via `set_move` but never replays history, so any enemy AI that consults `last_move`/`last_two_moves`/`move_history.len()` for gating (Mugger, BanditLeader, BronzeAutomaton, Sentry, Nemesis, Slavers, BookOfStabbing, Lagavulin, SlimeBoss, Hexaghost, Guardian, AwakenedOne) produces post-snapshot intents that diverge from an unsnapshotted engine. P0 merge-blocker. Fix scope ~15 LOC: add `move_history: Vec<i32>` field; populate from `enemy.move_history.clone()`; restore by iterating `push_move_history(id)` before final `set_move`. Stage H pre-merge audit (training-infra scratch, 2026-04-21). | **open** |
| D164 | combat_state_hash ignores RNG streams (merge-blocker F3) | bug | `packages/engine-rs/src/search.rs:1087-1138` | n/a (MCTS transposition invariant) | `combat_state_hash` hashes player HP/block/energy/orbs/stance, draw/discard/exhaust piles, hand, enemy intents + statuses — but NOT `engine.rng` or `engine.ai_rng`. Two search nodes with identical gameplay surface but different RNG stream positions hash identically, so the MCTS transposition table conflates genuinely distinct futures (different enemy AI rolls, different card draws, different damage rolls) and returns stale evaluations. Compounds with F1/F2: even if snapshot serialization is fixed, the hash still won't distinguish. P0 merge-blocker. Fix scope ~6 LOC: `engine.rng.state_tuple().hash(&mut h); engine.ai_rng.state_tuple().hash(&mut h);` before the final write. Stage H pre-merge audit (training-infra scratch, 2026-04-21). | **open** |
| D165 | Snapshot roundtrip test asserts gameplay surface only (merge-blocker F4 coverage gap) | bug | `packages/engine-rs/src/training_contract.rs:1522-1545` (roundtrip test) | n/a (training-contract test) | The existing snapshot roundtrip test asserts equality on `hp/block/energy/enemies[].hp/enemies[].intent` but never asserts `ai_rng.state_tuple()` equality or post-resume move_id determinism. F1/F2/F3 bugs can land silently because no test covers them. P0 coverage gap; blocks safe remediation of F1-F4. Fix scope: create `test_snapshot_determinism.rs` (~40 LOC) with "snapshot → resume → advance one enemy turn; assert move_id and ai_rng state match a twin engine that skipped the roundtrip." Red test drives F1/F2/F3 fix validation. Stage H pre-merge audit (training-infra scratch, 2026-04-21). | **open** |
| D166 | Flight status no atStartOfTurn restoration (decrements once then never resets) | bug | `packages/engine-rs/src/engine.rs:2479-2485` (Flight application site) + missing hook in `start_player_turn` L1085-1114 | `decompiled/java-src/com/megacrit/cardcrawl/powers/FlightPower.java::atStartOfTurn` | Java Flight restores stack to `flightInit` (3) each `atStartOfTurn`; once exhausted in a turn, it refills next turn. Rust applies Flight once and only decrements on hit — never restores at start of turn. After one hit-through, Flight stays at the post-hit level forever until re-applied. Priority P1 (Flight is a Watcher core defensive; high impact on Watcher A0 training). Fix scope ~10 LOC: wire Flight atStartOfTurn into the dead-dispatch `process_start_of_turn` pipeline (Cycle 3) or direct restore in `start_player_turn`. Stage H pre-merge audit (powers-relics scratch, 2026-04-21). | **open** |
| D167 | Angry triggers on HP_LOSS and Thorns retaliation (should attack-only) | bug | `packages/engine-rs/src/combat_hooks.rs:578-583` | `decompiled/java-src/com/megacrit/cardcrawl/powers/AngryPower.java::onAttacked` | Java Angry gates on `damageAmount > 0 && damageType == DamageInfo.DamageType.NORMAL` (attack only). Rust's Angry hook fires on every `on_enemy_damaged` path including HP_LOSS (Bane, Burn, Orange Pellets rebound) and Thorns retaliation against the attacker. Result: enemies with Angry (TheCollector mainly) gain Strength from every HP-loss source, not just player attacks. Priority P1 (TheCollector is Act 2 boss; Watcher A0 training-path). Fix scope ~4 LOC: gate Angry hook on `damage_type == DamageType::Normal` before the Strength increment. Stage H pre-merge audit (combat-hooks scratch, 2026-04-21). | **open** |
| D168 | Buffer continue-skip bypasses Thorns retaliation (skips to next attack instead of absorbing damage and running through retaliation) | bug | `packages/engine-rs/src/combat_hooks.rs:162-166` | `decompiled/java-src/com/megacrit/cardcrawl/powers/BufferPower.java::onAttacked` | Java Buffer: `damageAmount > 0` → decrement Buffer, return `0`; Thorns/retaliation hooks still fire for the 0-damage hit. Rust: `continue` statement skips the entire per-hit loop iteration including the Thorns retaliation branch. Net: Awakened One (4 Buffer) absorbs a hit but never retaliates with Beat-of-Death / Thorns that Java would emit. Priority P1 (AwakenedOne is Act 3 boss; structural combat-pipeline deviation). Fix scope ~8 LOC: replace `continue` with explicit 0-damage assignment + fall-through to retaliation branch. Stage H pre-merge audit (combat-hooks scratch, 2026-04-21). | **open** |
| D169 | Player Intangible drops one turn early (justApplied flag missing) | bug | `packages/engine-rs/src/engine.rs:1578-1583` (end_turn intangible decrement) | `decompiled/java-src/com/megacrit/cardcrawl/powers/IntangiblePlayerPower.java::atEndOfRound` + `justApplied` pattern | Java Intangible sets `justApplied=true` on application; `atEndOfRound` skips decrement if `justApplied`, then resets flag. Net: applied on turn N → effective turn N+1. Rust end_turn decrements Intangible unconditionally, so a turn-N Apparition is gone by turn N+1 end (Java would still have it at end of N+1). Priority P1 (Apparition is a key Watcher defensive; Watcher A0 training-path). Fix scope ~6 LOC: add `justApplied` tracking on Intangible apply + gate end_turn decrement on it. Pattern already exists for debuffs via `apply_debuff_from_enemy`. Stage H pre-merge audit (powers-relics scratch, 2026-04-21). | **open** |
| D170 | Thorns retaliation has no attacker-Intangible cap | bug | `packages/engine-rs/src/combat_hooks.rs:240-251` | `decompiled/java-src/com/megacrit/cardcrawl/powers/ThornsPower.java::onAttacked` + `DamageInfo.applyPowers` | Java Thorns damage passes through the attacker's power chain, including Intangible's 1-damage cap. Rust's Thorns retaliation (`apply_thorns_damage_to_attacker` at L240-251) applies the raw thorns value directly without routing through the attacker's Intangible check. Result: a Thorns-3 hit against an Intangible player deals 3 HP instead of Java's 1. Priority P1 (affects all Watcher Apparition-turn interactions with thorns enemies — Byrds, TorchHeads, Spiker, Reptomancer snakes). Fix scope ~8 LOC: route thorns through `apply_player_powers_to_incoming` / `apply_enemy_powers_to_incoming` before subtraction. Stage H pre-merge audit (combat-hooks scratch, 2026-04-21). | **open** |
| D171 | Buffer no damageAmount > 0 guard (consumes stack on 0-damage attacks) | bug | `packages/engine-rs/src/combat_hooks.rs:160-166` | `decompiled/java-src/com/megacrit/cardcrawl/powers/BufferPower.java::onAttacked` | Java Buffer guards `if (damageAmount > 0) { amount--; return 0; }` — 0-damage attacks (blocked-completely, Intangible-capped-to-1-then-blocked, scry-damage=0) don't consume Buffer. Rust consumes Buffer on every hit including those that dealt 0 damage after block, so a fully-blocked hit against a Buffered enemy wastes a Buffer charge. Priority P1 (AwakenedOne's 4 Buffer stack can be stripped faster in Rust than Java — accelerates phase 2 significantly). Fix scope ~3 LOC: add `damage_amount > 0` guard to Buffer decrement. Pairs with D168 fix. Stage H pre-merge audit (combat-hooks scratch, 2026-04-21). | **open** |

## Adding a new row — quick template

```markdown
| Dn | Title | unverified | `path/file.rs:line` | `decompiled/.../File.java:line` | Symptom + speculative fix + scope | open |
```

After investigation, change `unverified` to `bug`/`intentional`/`deferred` and add a `Status` link to either the fixing commit or a DESIGN_DECISIONS update.

## Future automation (audit §6 stretch)

A `scripts/audit-deviations.sh` would:
- Diff Rust enemy IDs (extracted from `enemies/mod.rs::roll_next_move` match arms) vs Java monster classes in `decompiled/java-src/com/megacrit/cardcrawl/monsters/`. Any diff that's not in this register is a new `unverified` row.
- Diff Rust card IDs vs Java card pools.
- Diff Rust event IDs vs Java event registries.
- Output a delta to `docs/work_units/deviations-delta.md` and fail CI if anything new appeared without a register entry.

That's a follow-up work unit; for now the manual register is the source of truth and the discipline is "every PR adds rows for any new divergence it touches."
