# Pending goldens

Minted, deterministic (A/B verified) autoplay traces awaiting promotion to
../java/ once trace_replay maps their full action vocabulary (CHOOSE, PROCEED,
CANCEL -> RunAction decision surfaces). Promotion = move file + green suite.
