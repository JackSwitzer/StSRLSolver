This directory holds tests for the quarantined legacy training stack.

They are preserved for reference while the new combat-first training rebuild is
implemented in `packages/training` and validated by fresh tests under
`tests/training`.

Do not add new coverage here. New tests should target the rebuilt stack.
