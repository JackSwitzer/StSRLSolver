These scripts belong to the archived pre-rebuild training stack.

They are intentionally separated from the new training entrypoints so the
stacked training branch can build a clean combat-first workflow on top of the
audited Rust engine base.
