# Archived: old RL-era Claude skills + training automation (2026-07-10)

Skills (/pretrain, /experiment, /training-status, /data-audit, /training-text,
/morning-review, /codex-review) and scripts (alert.sh, continuous_training.sh)
targeted the pre-oracle RL training loop whose runs/logs were purged 2026-07-10.
They referenced now-deleted paths (logs/active, logs/runs). Superseded by the
verification-sweep world: AGENTS.md + docs/goal/ + scripts/{ledger,extract,trace_*}.
Archived per repo data policy, not deleted.
